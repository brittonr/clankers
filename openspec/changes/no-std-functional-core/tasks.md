## 1. Establish the no-std core boundary

- [ ] 1.1 Add characterization tests around the current session-command / prompt-lifecycle slice so busy gating, loop continuation, and thinking/tool-filter behavior are pinned before extraction
- [ ] 1.2 Create `crates/clankers-core` with `#![no_std]`, `alloc`, and only core-friendly dependencies/features
- [ ] 1.3 Define the first-slice `CoreState`, `CoreInput`, `CoreEffect`, `CoreOutcome`, and `CoreError` types in `clankers-core`, keeping the full public core surface free of raw `DaemonEvent`, `AgentEvent`, Tokio, or transport/protocol types
- [ ] 1.4 Add a workspace rail around `cargo check -p clankers-core --no-default-features --target thumbv7em-none-eabi` (or one repo-defined bare-metal equivalent) so `clankers-core` proves it compiles in `no_std` mode
- [ ] 1.5 Add `scripts/check-clankers-core-surface.sh` as a persistent repo rail over `crates/clankers-core/src/**/*.rs`, rejecting raw `clankers_protocol::DaemonEvent`, `clankers_agent::events::AgentEvent`, `clankers_protocol::SessionCommand`, `tokio::`, and transport/protocol type references inside public `CoreState`, `CoreInput`, `CoreEffect`, `CoreOutcome`, `CoreError`, and related exported core boundary code
- [ ] 1.6 Add `scripts/check-clankers-core-boundary.sh` as a persistent repo rail over `crates/clankers-core/Cargo.toml` plus `crates/clankers-core/src/**/*.rs`, enforcing the authoritative denylist (`tokio`, `tokio-util`, `crossterm`, `ratatui`, `redb`, `reqwest`, `iroh`, `portable-pty`, `std::fs`, `std::net`, `std::process`, `std::env`, `std::time`, `chrono::`, `tokio::`, `crossterm::`, `ratatui::`, `redb::`, `reqwest::`, `iroh::`)
- [ ] 1.7 Add a dedicated determinism validation target (for example `crates/clankers-core/tests/determinism.rs` or an equivalent named test target) that replays identical migrated-slice state/input pairs twice and fails on divergent next-state/effect results
- [ ] 1.8 Wire `cargo check -p clankers-core --no-default-features --target thumbv7em-none-eabi`, `scripts/check-clankers-core-boundary.sh`, `scripts/check-clankers-core-surface.sh`, and the dedicated determinism rail into the repo's ordinary validation path so those rails run continuously and block acceptance on failure
- [ ] 1.9 Wire the migrated reducer suite and shell-parity suites into the repo's ordinary validation path so continuous validation covers reducer and adapter regressions, not just boundary rails

## 2. Extract deterministic orchestration logic

- [ ] 2.1 Move `SessionCommand::Prompt`, prompt start/completion, `StartLoop` / `StopLoop`, `notify_prompt_done()`, and `check_post_prompt()` decisions into pure transition functions in `clankers-core`
- [ ] 2.2 Move `SetThinkingLevel`, `CycleThinkingLevel`, `SetDisabledTools`, and related thinking/tool-filter state changes for the same slice into the core transition layer
- [ ] 2.3 Add core-minted correlation identities for `StartPrompt`, `ApplyToolFilter`, and `RunLoopFollowUp`, using one unambiguous pending-work namespace plus at most one pending slot of each kind across the first slice, and rejecting mismatches with typed failures that leave state unchanged
- [ ] 2.4 Implement explicit out-of-order runtime-result rejection paths in the migrated slice, not just token-mismatch rejection
- [ ] 2.5 Reject conflicting `StartLoop` / `StopLoop` inputs while `RunLoopFollowUp` is pending, with unchanged-state assertions
- [ ] 2.6 Migrate logical session-event emission into `CoreEffect` and shell adapter execution for the first slice

## 3. Rewire the imperative shells

- [ ] 3.1 Add adapter code in `clankers-controller` and `clankers-agent` that translates shell commands/runtime results into `CoreInput`, keeping temporary `clankers-message` and other shell-native type translations outside `clankers-core`
- [ ] 3.2 Execute returned `CoreEffect` values from the shells without re-deriving orchestration policy outside `clankers-core`
- [ ] 3.3 Remove duplicated migrated decision logic from the shell paths and add a short boundary note in `crates/clankers-core/README.md` describing what future logic belongs in `clankers-core` versus shell adapters

## 4. Verify behavior and boundary integrity

- [ ] 4.1 Add positive reducer tests that prove identical state + input pairs produce identical next state and effect plans for `SessionCommand::Prompt`, `StartLoop`, `StopLoop`, `notify_prompt_done()`, `check_post_prompt()`, `SetThinkingLevel`, `CycleThinkingLevel`, and `SetDisabledTools`
- [ ] 4.2 Add matching and mismatched correlation-token round-trip tests for `StartPrompt` ↔ `PromptCompleted`, `RunLoopFollowUp` ↔ `LoopFollowUpCompleted`, and `ApplyToolFilter` ↔ `ToolFilterApplied`, including empty-slot, wrong-kind, duplicate, stale, repeated-busy-start, loop-already-active, loop-not-active, pending-follow-up loop-control rejection, stale pending tool-filter rebuild, explicit prompt/loop-follow-up failure outcomes, and explicit out-of-order rejection cases, verifying typed rejection behavior and unchanged state
- [ ] 4.3 Extend `crates/clankers-controller/src/command.rs` parity coverage for `SetThinkingLevel`, `CycleThinkingLevel`, concurrent-prompt rejection, disabled-tool updates, filtered-tool rebuild application, logical session-event emission, and `StartLoop` / `StopLoop` through the migrated reducer path, explicitly asserting preserved `DaemonEvent::SystemMessage` and `DaemonEvent::DisabledToolsChanged` acknowledgement/notification categories, the exact `CycleThinkingLevel` order `Off → Low → Medium → High → Max → Off`, unchanged-state error handling for invalid `SetThinkingLevel` inputs, successful `StartLoop` visible-state activation plus no immediate acknowledgement event, successful `StopLoop` visible transition plus success `DaemonEvent::SystemMessage`, preserved `StartLoop` / `StopLoop` error acknowledgement categories, `DaemonEvent::DisabledToolsChanged` before `DaemonEvent::SystemMessage` on successful disabled-tool application, and stale-slot `SetDisabledTools` rejection with error `DaemonEvent::SystemMessage` plus no `DaemonEvent::DisabledToolsChanged`
- [ ] 4.4 Extend `crates/clankers-controller/src/auto_test.rs` parity coverage for prompt completion, loop continuation, loop follow-up completion, and post-prompt follow-up decisions, plus one `SessionController::handle_command(SessionCommand::Prompt { .. })` shell-seam regression that asserts preserved prompt success/no-ack behavior, successful `LoopFollowUpCompleted` visible loop-state transition, failed `PromptCompleted` busy clearing, no success acknowledgement, suppressed `PostPromptAction::{ContinueLoop, RunAutoTest}`, error `DaemonEvent::SystemMessage` when an active loop fails, emitted events, busy-state transitions, `PostPromptAction` categories, loop-follow-up failure `DaemonEvent::SystemMessage`, and stale/wrong completion rejection through the controller adapter seam
- [ ] 4.5 Extend `crates/clankers-agent/src/turn/mod.rs` parity coverage for migrated tool-filter behavior and add one `crates/clankers-agent/src/lib.rs` regression for thinking/tool-filter adapter behavior through `clankers-core`
- [ ] 4.6 Run `cargo check -p clankers-core --no-default-features --target thumbv7em-none-eabi`, the dedicated determinism rail, the persistent `CoreEffect` boundary check, the banned-dependency/API rail, and the migrated test suite
- [ ] 4.7 Perform an anti-fork review over `crates/clankers-controller/src/{command.rs,auto_test.rs}` and `crates/clankers-agent/src/{lib.rs,turn/mod.rs}` to verify migrated-slice policy now lives in `clankers-core` and shell code is translation/effect execution only
- [ ] 4.8 Capture any follow-up extraction limits or seams discovered during verification
