## Phase 1: Baseline and Extraction

- [x] [serial] Inventory self_evolution.rs responsibilities across run, approve, apply, rollback, receipt validation, candidate IO, hashes, and verification. ✅ Identified receipt/options models, fake executor seam, run/approval/apply/rollback shells, application receipt-chain guards, hash/path helpers, and existing stale-target/rollback CLI regressions (completed: 2026-05-13T00:02:14Z).
- [x] [depends:baseline] Extract receipt models/validators and hash/path guards with negative tests. ✅ Receipt/option/data models extracted into `src/self_evolution/receipts.rs`; receipt-chain validators, application/approval/rollback receipt readers, path guards, artifact identity, and SHA-256 helpers extracted into `src/self_evolution/validation.rs`; root re-exports preserve existing API; self-evolution and CLI negative tests cover stale targets, mismatched approvals, dry-run boundaries, rollback guards, invalid manifests, and non-promotable receipts; `nix develop -c 'CARGO_TARGET_DIR=target cargo fmt --check -p clankers && CARGO_TARGET_DIR=target cargo nextest run -p clankers self_evolution --no-fail-fast && CARGO_TARGET_DIR=target cargo check --tests -p clankers'` passed 26/26; `openspec validate decompose-self-evolution --strict --json` and `git diff --check` passed (started: 2026-05-13T00:02:14Z → completed: 2026-05-13T00:09:30Z).
- [x] [depends:baseline] Extract run/approval/apply/rollback orchestration shells around pure validators. ✅ Split orchestration shells into `src/self_evolution/run.rs`, `approval.rs`, `application.rs`, and `rollback.rs`; root module now preserves compatibility with public re-exports while tests still exercise the original CLI/API paths; `nix develop -c 'CARGO_TARGET_DIR=target cargo fmt --check -p clankers && CARGO_TARGET_DIR=target cargo nextest run -p clankers self_evolution --no-fail-fast && CARGO_TARGET_DIR=target cargo check --tests -p clankers'` passed 26/26; `openspec validate decompose-self-evolution --strict --json` and `git diff --check` passed (completed: 2026-05-13T00:23:39Z).
- [ ] [depends:baseline] Preserve CLI dogfood and stale-target/rollback regression coverage.
- [ ] [serial] Run cargo fmt, self_evolution lib and CLI/integration tests, cargo check --tests -p clankers, openspec validate, and git diff --check.
