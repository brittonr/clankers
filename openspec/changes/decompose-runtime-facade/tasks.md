## Phase 1: Baseline and Extraction

- [x] [serial] Inventory public types in `clankers-runtime/src/lib.rs` and group them into session, builder, services, tools, confirmation, prompt, and events modules. ✅ events group extracted first (completed: 2026-05-12T22:23:14Z)
- [x] [depends:baseline] Extract event/metadata/status types into `src/events.rs` while preserving root re-exports and docs. ✅ `events` module added with root compatibility re-exports (completed: 2026-05-12T22:23:14Z)
- [x] [depends:baseline] Add API compatibility tests proving old root event imports still compile. ✅ `crates/clankers-runtime/tests/api_compat.rs` covers root/module event paths (completed: 2026-05-12T22:23:14Z)
- [x] [depends:baseline] Run focused runtime/tool-host embedding tests and default-safe negative tests. ✅ `cargo nextest run -p clankers-runtime --no-fail-fast` passed 27/27; `cargo check --tests -p clankers-runtime -p clankers` passed in dev shell (completed: 2026-05-12T22:23:14Z)
- [~] [depends:baseline] Move remaining type groups into modules while preserving root re-exports and docs. ⏱ started: 2026-05-12T22:40:31Z; prompt/model request group extracted into `src/prompt.rs` with root compatibility re-exports; API compatibility test covers root/module prompt paths; `nix develop -c 'CARGO_TARGET_DIR=target cargo nextest run -p clankers-runtime --no-fail-fast && CARGO_TARGET_DIR=target cargo check --tests -p clankers-runtime -p clankers'` passed 28/28 (2026-05-12T22:40:31Z). Tool catalog group extracted into `src/tools.rs` with root compatibility re-exports; API compatibility test covers root/module tool catalog paths; `nix develop -c 'CARGO_TARGET_DIR=target cargo nextest run -p clankers-runtime --no-fail-fast && CARGO_TARGET_DIR=target cargo check --tests -p clankers-runtime -p clankers'` passed 29/29 (2026-05-12T22:43:57Z). Confirmation group extracted into `src/confirmation.rs` with root compatibility re-exports; API compatibility test covers root/module confirmation paths; `nix develop -c 'CARGO_TARGET_DIR=target cargo fmt --check -p clankers-runtime && CARGO_TARGET_DIR=target cargo nextest run -p clankers-runtime --no-fail-fast && CARGO_TARGET_DIR=target cargo check --tests -p clankers-runtime -p clankers'` passed 30/30 and `openspec validate decompose-runtime-facade --strict --json` passed (2026-05-12T22:53:49Z). Remaining groups: session, builder, services.
- [ ] [serial] Run final cargo fmt, cargo nextest -p clankers-runtime, cargo check --tests for runtime/embedding dependents, openspec validate, and git diff --check after all module groups are extracted.
