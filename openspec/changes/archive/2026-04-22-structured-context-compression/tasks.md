## 1. Tool-result pruning pre-pass

- [x] I1 [covers=structured.compaction.pruning.prepass, structured.compaction.pruning.prepass.bashtoolresult, structured.compaction.pruning.prepass.readtoolresult, structured.compaction.pruning.prepass.commonfiletools, structured.compaction.pruning.prepass.subagenttoolresult, structured.compaction.pruning.prepass.unknowntooltype] Create `crates/clankers-agent/src/compaction/tool_summaries.rs` and implement summarizers for core tools plus generic fallback: bash (command + exit code + line count), read (path + offset + char count), write (path + line count), grep/rg (pattern + match count), edit (path + edit count), subagent (goal + result size), and `[tool_name] arg1=val1 (N chars result)`
- [x] I2 [covers=structured.compaction.pruning.prepass.manualcompactsharedpath] Implement `prune_tool_results(messages, tail_start_idx) -> Vec<AgentMessage>` and thread the shared pruning helper through `context::compact_stale_tool_results`, `Agent::compact_messages`, and controller/manual `/compact` handling so standalone and daemon paths stop drifting

## 2. Token-budget tail protection

- [x] I3 [covers=structured.compaction.tail.budgetprotection, structured.compaction.tail.budgetprotection.contextwindowadapts, structured.compaction.tail.budgetprotection.shortrecentmessages] Add `compaction.tail_budget_fraction`, keep the auto-compaction threshold configurable/defaulted, implement `select_tail_by_budget(messages, budget_tokens) -> usize`, and replace fixed `keep_recent` count with budget-based tail selection in `AutoCompactConfig`

## 3. LLM-powered structured summarization

- [x] I4 [covers=structured.compaction.structured.summarization, structured.compaction.structured.summarization.triggeredsummary] Add `compaction.summary_model` config option and define the structured summary prompt template with sections: Active Task, Key Decisions Made, Files Modified, and Remaining Work
- [x] I5 [covers=structured.compaction.summary.handoffframing, structured.compaction.summary.handoffframing.modelreceivescompactedcontext] Define the `SUMMARY_PREFIX` handoff notice text and wire compacted output framing so the generated summary replaces middle messages with handoff guidance that treats the summary as background reference, forbids re-executing summarized work, and instructs the model to respond only to the latest user message
- [x] I6 [covers=structured.compaction.structured.summarization.triggeredsummary, structured.compaction.iterative.summaryupdates, structured.compaction.iterative.summaryupdates.secondcompaction] Implement `summarize_middle(messages, previous_summary, provider) -> String`, including previous-summary reuse during subsequent compactions

## 4. Fallback and strategy selection

- [x] I7 [covers=structured.compaction.structured.summarization.auxmodelunavailable, structured.compaction.structured.summarization.summarycallfallback] If auxiliary model is unavailable, or if the summarization call fails or times out, fall back to truncation-only compaction and run summarization with a 30-second timeout
- [x] I8 [covers=structured.compaction.structured.summarization] Update `CompactionStrategy` with `Structured` and make it the default only when an auxiliary summary model is configured

## 5. Persistence and recovery

- [x] I9 [covers=structured.compaction.iterative.summaryupdates, structured.compaction.iterative.summaryupdates.reopenrecovery] Store compaction summary text in `CompactionResult`, persist it in session state, restore it during reopen/resume seeding, and save compaction summaries to `clankers-db/tool_results` for recovery/debugging

## 6. Verification

- [x] V1 [covers=structured.compaction.pruning.prepass.bashtoolresult, structured.compaction.pruning.prepass.readtoolresult, structured.compaction.pruning.prepass.commonfiletools, structured.compaction.pruning.prepass.subagenttoolresult, structured.compaction.pruning.prepass.unknowntooltype] Unit test: tool-result summarizers produce correct one-liners for bash, read, write, grep/rg, edit, subagent, and unknown-tool fallback [evidence=openspec/changes/structured-context-compression/evidence/v1-pruning-summarizers.md]
- [x] V2 [covers=structured.compaction.pruning.prepass.manualcompactsharedpath] Regression coverage: `/compact` in both standalone and daemon-backed session paths uses the shared pruning path so older tool results are mutated while recent tail tool results stay intact [evidence=openspec/changes/structured-context-compression/evidence/v2-manual-compact-shared-path.md]
- [x] V3 [covers=structured.compaction.tail.budgetprotection, structured.compaction.tail.budgetprotection.contextwindowadapts, structured.compaction.tail.budgetprotection.shortrecentmessages] Unit test: tail budget selection preserves correct number of messages for various context sizes, including that short recent messages preserve more tail messages than fixed-count protection and that the default `tail_budget_fraction` is 0.40 [evidence=openspec/changes/structured-context-compression/evidence/v3-tail-budget-selection.md]
- [x] V4 [covers=structured.compaction.summary.handoffframing.modelreceivescompactedcontext] Unit test: handoff prefix is present in compacted output and the notice says the summary is background reference, forbids re-executing summarized work, and instructs the model to respond only to the latest user message [evidence=openspec/changes/structured-context-compression/evidence/v4-handoff-framing.md]
- [x] V5 [covers=structured.compaction.iterative.summaryupdates.secondcompaction, structured.compaction.iterative.summaryupdates.reopenrecovery, structured.compaction.structured.summarization.triggeredsummary, structured.compaction.structured.summarization.auxmodelunavailable] Integration test: full compaction pipeline with mock provider, including persisted-summary restore + reopen/resume seeding + second-compaction previous-summary reuse, explicit captured structured-summary prompt inspection (`## Previous Summary` + structured template + conversation excerpt), middle-message replacement by the generated summary, auxiliary-model-unavailable fallback, and persisted summary recovery from `clankers-db/tool_results` for debugging/recovery flows [evidence=openspec/changes/structured-context-compression/evidence/v5-embedded-integration.md]
- [x] V6 [covers=structured.compaction.structured.summarization.triggeredsummary, structured.compaction.structured.summarization.summarycallfallback] Deterministic verification: auxiliary-model failure and timeout fallback both degrade to truncation correctly, the summarization timeout is fixed at 30 seconds, and strategy selection chooses `Structured` only when an auxiliary summary model is configured [evidence=openspec/changes/structured-context-compression/evidence/v6-strategy-and-fallback.md]