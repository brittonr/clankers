## 1. Provider-aware auth plumbing

- [x] 1.1 Add a provider-aware OAuth driver abstraction in the shared auth layer so Anthropic and `openai-codex` login flows share the same command and slash-command plumbing
- [x] 1.2 Implement OpenAI Codex authorize, code-exchange, load/reload, refresh, account-ID derivation, and malformed/missing-claim rejection helpers, keep `chatgpt_account_id` derived from the active token instead of adding a persisted auth-store field, then store credentials under provider `openai-codex` and mark the requested `openai-codex` account active
- [x] 1.3 Isolate pending OAuth verifier and state storage by provider plus requested clankers account name from the existing `--account <name>` / slash-account syntax so Anthropic and `openai-codex` in-flight logins, or two `openai-codex` logins for different accounts, cannot overwrite each other
- [x] 1.4 Wire `openai-codex` into the proactive/background refresh path so refreshed credentials update the persisted auth store before expiry and reset in-memory entitlement state for re-evaluation without adding a persisted entitlement field
- [x] 1.5 Update `clankers auth` and `/login` / `/account` handlers to accept explicit provider names, implement provider-scoped switch/logout behavior, show grouped and provider-scoped status including provider-specific OAuth validity/expiry, preserve omitted-provider Anthropic defaults, reload the affected provider after a successful login, and invalidate/recompute entitlement state on provider reload or account switch
- [x] 1.6a Add tests for provider-aware auth parsing, pending-state isolation, and omitted-provider Anthropic defaults across CLI and slash-command login, status, switch, and logout flows
- [x] 1.6b Add tests for grouped and provider-scoped status output, separate OAuth validity/expiry reporting, provider-scoped switch/logout, and persistence while preserving existing `anthropic` / API-key `openai` credentials after `openai-codex` login
- [x] 1.6c Add tests for proactive/background refresh plus provider reload/account switch entitlement recomputation
- [x] 1.6d Add tests for token refresh, malformed/missing-claim rejection, request blocking without a valid account ID, account-ID derivation from loaded and refreshed credentials, and activation of the requested `openai-codex` account after login
- [x] 1.7 Add a deterministic auth-fixture check for the OpenAI OAuth contract: authorize URL base + required query parameters, token endpoint URL, and required exchange/refresh form fields

## 2. Request metadata plumbing

- [x] 2.1 Add `extra_params` to `crates/clankers-provider::CompletionRequest`, then audit every router-bound `CompletionRequest { ... }` construction path in `crates/clankers-agent/src/turn/execution.rs`, `src/modes/agent_task.rs`, `src/worktree/llm_resolver.rs`, `crates/clankers-provider/src/router.rs`, `crates/clankers-provider/src/rpc_provider.rs`, and router test/helper constructors so provider extras are preserved or explicitly initialized
- [x] 2.2 Pass `extra_params` through `RouterCompatAdapter` and `RpcProvider`, and thread the literal persisted clankers session identifier from interactive and agent entry points into request `extra_params` as `_session_id`
- [x] 2.3 Add tests that the `_session_id` key inside provider-specific `extra_params` survives both local and RPC provider paths, is reused across later turns, and is reused again after session resume
- [x] 2.4 Add a deterministic constructor-inventory check covering router-bound `CompletionRequest` construction in `crates/clankers-agent/src/turn/execution.rs`, `src/modes/agent_task.rs`, `src/worktree/llm_resolver.rs`, `crates/clankers-provider/src/router.rs`, `crates/clankers-provider/src/rpc_provider.rs`, and router test/helper constructors so new call paths cannot omit `extra_params` silently
- [x] 2.5 Add a deterministic cross-repo schema/serialization parity check covering `crates/clankers-provider::CompletionRequest` and `clanker-router::CompletionRequest` so `extra_params`, including the `_session_id` key inside that map, survive local and RPC transport boundaries unchanged

## 3. OpenAI Codex backend and discovery

- [x] 3.1 Add curated `openai-codex` model catalog entries, exact-set verification for the current ChatGPT-account model IDs (`gpt-5.3-codex`, `gpt-5.3-codex-spark`) and no extras, entitlement evaluation/state, and discovery/status logic that only exposes the provider when an `openai-codex` account is both authenticated and entitled, triggers probing from discovery plus grouped/provider-scoped status when entitlement is unknown, classifies HTTP 403 and structured error code `usage_not_included` as not entitled, surfaces authenticated-but-not-entitled or entitlement-check-failed state for unsupported accounts, and keeps entitlement state in memory only
- [x] 3.2 Implement a dedicated OpenAI Codex backend in `clanker-router` for the ChatGPT Codex Responses SSE transport, including request conversion for current-turn user input, ordered prior user-turn replay, prior assistant text/refusal items, `instructions` = system prompt only, original tool call/item IDs, paired `function_call_output` items, auth-layer-provided account-ID header injection, auth headers, `reasoning.encrypted_content`, `text={"verbosity":"medium"}` by default with caller override support, entitlement probes against `POST https://chatgpt.com/backend-api/codex/responses`, and session cache key handling
- [x] 3.3 On explicit or resumed `openai-codex` requests, trigger the entitlement probe first when state is `unknown`, send the normal Codex turn only after a successful probe, fail closed with an explicit ChatGPT Plus/Pro-required message when the account is `not_entitled`, fail closed with a retriable provider error plus entitlement-check-failed semantics when the probe cannot classify the account, and never fall back to API-key `openai`
- [x] 3.4 Map raw Codex SSE events `response.output_item.added`, `response.reasoning_summary_part.added`, `response.reasoning_summary_text.delta`, `response.reasoning_summary_part.done`, `response.content_part.added`, `response.output_text.delta`, `response.refusal.delta`, `response.function_call_arguments.delta`, `response.function_call_arguments.done`, `response.output_item.done`, and `response.completed` into generic `StreamEvent` boundaries, including exactly-once `MessageStart`, `ContentBlockStart`, `ContentBlockDelta`, `ContentBlockStop`, `MessageDelta`, and `MessageStop` for text, thinking, and tool-use flows; ensure `response.reasoning_summary_part.added/done` do not create extra generic `ContentBlockStart/Stop` boundaries; map `completed` to a normal stop, `incomplete` to the generic incomplete/max-tokens stop path, treat `failed` and `cancelled` as provider errors instead of successful `MessageStop`, and suppress `queued` and `in_progress` as final stop reasons; carry tool-call correlation as `<call_id>|<item.id>` plus tool name; replay prior-turn reasoning from `Content::Thinking.signature`; and exclude display-only thinking text from replay payloads
- [x] 3.5 Implement the chosen retry envelope for Codex requests: up to 3 transient retries on 429 or retryable 5xx with 1s/2s/4s backoff, plus exactly one reactive 401 refresh-and-retry, continue with the remaining transient budget after refresh, and never perform more than one refresh cycle per top-level request
- [x] 3.6 Add tests for discovery and model resolution with entitled, missing, and unsupported `openai-codex` accounts, including user-visible provider-qualified catalog entries, authenticated-but-not-entitled and entitlement-check-failed status output, status-triggered probing when entitlement is unknown, explicit/resumed request blocking for both unsupported-account and entitlement-check-failed paths, while API-key `openai` behavior stays unchanged
- [x] 3.7a Add fixture-backed unit and integration tests for normal request mapping: first-turn current-user input, ordered prior user-turn replay, assistant text/refusal replay, original tool call/item IDs, paired `function_call_output` items, reasoning signatures, signature replay without resending display-only thinking text, and session cache key propagation
- [x] 3.7b Add fixture-backed unit and integration tests for Codex stream normalization: exact generic stream boundaries (`MessageStart`, `ContentBlockStart/Delta/Stop`, `MessageDelta`, `MessageStop`), raw SSE event handling for `response.output_item.added` / `response.reasoning_summary_part.added` / `response.reasoning_summary_text.delta` / `response.reasoning_summary_part.done` / `response.content_part.added` / `response.output_text.delta` / `response.refusal.delta` / `response.function_call_arguments.delta` / `response.function_call_arguments.done` / `response.output_item.done` / `response.completed -> MessageDelta + MessageStop`, explicit verification that `response.reasoning_summary_part.added/done` do not create extra generic `ContentBlockStart/Stop` boundaries, tool-call correlation, usage normalization, and stop-reason mapping including `incomplete` -> generic incomplete/max-tokens stop plus `failed`/`cancelled` provider-error behavior and suppression of `queued`/`in_progress` as final stop reasons
- [x] 3.7c Add fixture-backed retry and entitlement-classification tests covering direct no-retry behavior for non-401 4xx responses, retry-envelope behavior including exact `1s/2s/4s` no-jitter delays plus combined 401+transient sequences and remaining-budget checks, classification of HTTP 403 and `usage_not_included` as not entitled, and unknown-entitlement handling after probe failure
- [x] 3.7d Add at least one real raw-SSE runtime seam test at the actual parser entrypoint rather than only helper-level wrappers
- [x] 3.8 Add a deterministic request-fixture check that normal Codex requests send required headers (`Authorization`, `chatgpt-account-id`, `OpenAI-Beta`, `originator`, `accept`, `content-type`, and `session_id` when applicable) on initial request, transient retry, and 401 refresh-retry paths
- [x] 3.9 Add a deterministic normal-request body fixture check for `POST https://chatgpt.com/backend-api/codex/responses` on initial, transient-retry, and 401 refresh-retry paths: required `model`, `store=false`, `stream=true`, `instructions` containing only the system prompt, ordered `input` replay for prior user turns and assistant text/refusal turns, `text={"verbosity":"medium"}` by default with caller override verification, `include=["reasoning.encrypted_content"]`, `tool_choice="auto"`, `parallel_tool_calls=true`, optional `tools`/`reasoning`, and `prompt_cache_key` when `_session_id` is present
- [x] 3.10 Add deterministic entitlement-probe fixture checks for initial, transient-retry, and 401 refresh-retry probe paths: `POST https://chatgpt.com/backend-api/codex/responses`, required auth/account headers plus `OpenAI-Beta: responses=experimental`, `originator`, `accept: text/event-stream`, and `content-type`, explicit omission of `session_id`, `model=gpt-5.3-codex`, `store=false`, `stream=true`, minimal `instructions`/`input`/`text`, no tools, no prompt-cache key, and no session-specific state
- [x] 3.11 Add a deterministic regression check that API-key `openai` keeps its existing transport and omits Codex-only headers and session semantics
- [x] 3.12 Update the extracted `clanker-router` dependency or pin in this workspace after the backend and auth changes land there, including any required `crate-hashes.json` and `flake.nix` source updates for Nix builds

## 4. UX, docs, and verification

- [x] 4.1 Update CLI help, slash help, provider lists, and model-selection docs to describe `openai-codex` login, account naming via existing `--account` / slash-account syntax, personal-use/plan limitations, unsupported-plan behavior, and the separation from API-key `openai`
- [x] 4.2 Add a docs/help acceptance or snapshot check proving CLI help, slash help, and provider docs cover `openai-codex` login, account naming, model selection, personal-use/plan limitations, unsupported-plan behavior, and unchanged API-key `openai` help paths
- [x] 4.3 Add or update recorded-fixture end-to-end coverage for login, status/account switching, interactive credential reload, model resolution, and one Codex turn where live credentials are not available in CI
- [x] 4.4 Run `cargo nextest run`, `cargo clippy -- -D warnings`, and `nix build .#clankers` after the workspace and extracted-crate updates are in place
- [x] 4.5 After fixture and automated verification pass, run one manual live-credential smoke path against a real ChatGPT Plus/Pro subscription using an already-authenticated account: verify provider-scoped status, account switching, model resolution, and one Codex turn. Fresh login and interactive reload stay covered by the recorded-fixture and automated auth tests.
- [x] 4.6 Record the manual live-credential smoke result in the implementation PR or change notes before the change is marked ready

## Verification evidence

- Router-side work for tasks `3.2`, `3.8`, and `3.10` landed in sibling repo `../clanker-router` commit `88aad21` (`openai-codex: align request contracts`).
- Router-side deterministic normal-request body fixtures for task `3.9` landed in sibling repo `../clanker-router` commit `d078137` (`codex: fixture normal request bodies`).
- Router-side stream normalization verification for tasks `3.4`, `3.7b`, and `3.7d` landed in sibling repo `../clanker-router` commit `feb597a` (`codex: verify stream normalization`).
- Router-side entitlement-gating, request-mapping, and retry-envelope verification for tasks `3.3`, `3.5`, `3.7a`, and `3.7c` landed in sibling repo `../clanker-router` commit `898e144` (`codex: harden probe and retry fixtures`).
- Router-side live-contract alignment for the ChatGPT-account Codex catalog, streaming entitlement probe, provider-prefix model normalization, and manual-smoke unblocking landed in sibling repo `../clanker-router` commit `eed716b` (`codex: align live chatgpt contract`).
- Workspace pin/update for task `3.12` now uses a reproducible vendored snapshot at `vendor/clanker-router/`, sourced from `https://github.com/brittonr/clanker-router` commit `eed716b` (`codex: align live chatgpt contract`), with `Cargo.toml`, `flake.nix`, and `flake.lock` updated to consume that in-repo source.
- Current-repo discovery/status coverage for task `3.6` landed in `crates/clankers-provider/src/discovery.rs`, `crates/clankers-provider/src/openai_codex.rs`, and `src/commands/auth.rs`, with explicit/entitlement-failure current-repo router shaping already covered in `crates/clankers-provider/src/router.rs`.
- Current-repo regression rail for task `3.11` landed in `crates/clankers-provider/src/router.rs` as `test_openai_compat_transport_omits_codex_headers_and_session_semantics`.
- Docs/help updates for tasks `4.1` and `4.2` landed in `src/cli.rs`, `src/slash_commands/handlers/auth.rs`, `README.md`, `docs/src/getting-started/auth.md`, `docs/src/reference/commands.md`, and `tests/openai_codex_help_docs.rs`.
- Recorded current-repo fixture flow coverage for task `4.3` landed in `tests/openai_codex_recorded_flow.rs`, `tests/fixtures/openai_codex/{work-export,backup-export}.json`, `src/slash_commands/handlers/auth.rs`, and `crates/clankers-provider/src/discovery.rs`. The recorded integration test proves: (1) `auth login --provider openai-codex --account work` persists provider-scoped pending-login state under `.login_verifiers/openai-codex/work.json`; (2) importing the `work` and `backup` fixture exports populates provider-scoped status output with both fixture labels; and (3) `auth switch --provider openai-codex backup` flips the active account shown by `auth status --provider openai-codex`.
- Full workspace verification for task `4.4` passed via `cargo nextest run`, `cargo clippy -- -D warnings`, and `nix build .#clankers`. The nextest rerun also stabilized `tests/tui/tmux_smoke.rs` styled snapshot normalization so cwd truncation no longer flakes the 24x80 snapshot.
- Manual live-credential smoke note for tasks `4.5`/`4.6`: a first live run exposed contract drift (`gpt-5.1/5.2` ChatGPT-account models returned HTTP 400 unsupported-model; `gpt-5.3-codex` / `gpt-5.3-codex-spark` required `stream=true` for the entitlement probe). The router/backend contract, current-repo helper, docs/help examples, and OpenSpec artifacts were updated to the live `gpt-5.3-*` + streaming-probe contract, then a rerun against an isolated temp auth store succeeded for provider-scoped status, `auth switch --provider openai-codex work`, model resolution, and a live turn returning `LIVE-CODEX-SMOKE-OK`. Fresh login + interactive reload remained covered by `tests/openai_codex_recorded_flow.rs` and the slash-auth/provider reload automated tests.
- Validation commands run for this slice:
  - `cd ../clanker-router && cargo test backends::openai_codex::tests -- --nocapture`
  - `cd ../clanker-router && cargo test build_codex_request_ -- --nocapture`
  - `cd ../clanker-router && cargo test codex_stream_ -- --nocapture`
  - `cd ../clanker-router && cargo test parse_codex_sse_runtime_seam_normalizes_raw_events -- --nocapture`
  - `cd ../clanker-router && cargo test codex_completed_ -- --nocapture`
  - `cd ../clanker-router && cargo test build_codex_input_ -- --nocapture`
  - `cd ../clanker-router && cargo test live_probe_does_not_retry_non_401_4xx -- --nocapture`
  - `cd ../clanker-router && cargo test live_probe_retries_retryable_statuses_with_deterministic_backoff -- --nocapture`
  - `cd ../clanker-router && cargo test openai_codex_attempt_does_not_retry_non_401_4xx -- --nocapture`
  - `cd ../clanker-router && cargo test openai_codex_attempt_refreshes_only_once_and_preserves_remaining_transient_budget -- --nocapture`
  - `cd ../clanker-router && cargo test ensure_entitlement_keeps_unknown_after_probe_failure -- --nocapture`
  - `cd ../clanker-router && cargo test openai_codex_provider_ -- --nocapture`
  - `cargo test -p clankers-provider build_router_ -- --nocapture`
  - `cargo test -p clankers-provider explicit_codex_request_ -- --nocapture`
  - `cargo test -p clankers-provider codex_status_suffix_ -- --nocapture`
  - `cargo test -p clankers-provider test_routed_codex_not_entitled_error_survives_current_repo_router -- --nocapture`
  - `cargo test -p clankers-provider test_routed_codex_probe_failure_error_survives_current_repo_router -- --nocapture`
  - `cargo test -p clankers-provider test_openai_compat_transport_omits_codex_headers_and_session_semantics -- --nocapture`
  - `cargo test -p clankers-provider recorded_codex_model_resolution_and_turn_flow_uses_routed_backend -- --nocapture`
  - `cargo test -p clankers-provider -- --nocapture`
  - `cargo test -p clankers login_complete_dispatches_provider_scoped_agent_command -- --nocapture`
  - `cargo test -p clankers provider_logout_triggers_interactive_reload_credentials -- --nocapture`
  - `cargo test --test openai_codex_help_docs -- --nocapture`
  - `cargo test --test openai_codex_recorded_flow -- --nocapture`
  - `cargo nextest run`
  - `cargo clippy -- -D warnings`
  - `nix build .#clankers -L`
  - `CLANKERS_AUTH_FILE=<tmp> CLANKERS_NO_DAEMON=1 cargo run --quiet -- auth import --input <live-export.json>`
  - `CLANKERS_AUTH_FILE=<tmp> CLANKERS_NO_DAEMON=1 cargo run --quiet -- auth status --provider openai-codex`
  - `CLANKERS_AUTH_FILE=<tmp> CLANKERS_NO_DAEMON=1 cargo run --quiet -- auth switch --provider openai-codex work`
  - `CLANKERS_AUTH_FILE=<tmp> CLANKERS_NO_DAEMON=1 cargo run --quiet -- -p 'Reply with LIVE-CODEX-SMOKE-OK only.' --model openai-codex/gpt-5.3-codex`
  - `cargo test -p clankers-provider test_compat_adapter_preserves_session_id_extra_param -- --nocapture`
  - `cd ../clanker-router && cargo test build_codex_request_ -- --nocapture`
  - `cd ../clanker-router && cargo test openai_codex_provider_probes_before_first_normal_request -- --nocapture`
