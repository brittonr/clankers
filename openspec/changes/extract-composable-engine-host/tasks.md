## Phase 0: Readiness

- [x] R1 Confirm archived prerequisite cleanup from `decouple-llm-contract-surface` and `separate-engine-core-composition`, or equivalent dependency rails green, before implementation by recording command output for `./scripts/check-llm-contract-boundary.sh`, `cargo test -p clankers-controller --test fcis_shell_boundaries`, and `openspec validate extract-composable-engine-host --strict`. [covers=embeddable-agent-engine.composable-host-contract,embeddable-agent-engine.host-extraction-rails,embeddable-agent-engine.core-engine-boundary-rails] [evidence=openspec/changes/extract-composable-engine-host/evidence/readiness-r1.md]
- [x] R2 Keep proposal/design/tasks gates green before implementation tasks are marked complete. [covers=embeddable-agent-engine.composable-host-contract,embeddable-agent-engine.reusable-tool-host,embeddable-agent-engine.reusable-stream-accumulator,embeddable-agent-engine.host-extraction-rails] [evidence=openspec/changes/extract-composable-engine-host/evidence/readiness-r2.md]

### Phase 0 Evidence Summary

- `./scripts/check-llm-contract-boundary.sh`: PASS (`ok: clankers-engine normal-edge tree excludes forbidden crates`; `ok: clanker-message normal-edge tree excludes forbidden crates`; `ok: crates/clankers-engine/src excludes forbidden source tokens`).
- `cargo test -p clankers-controller --test fcis_shell_boundaries`: PASS (28 passed, 0 failed).
- `openspec validate extract-composable-engine-host --strict`: PASS (`Change 'extract-composable-engine-host' is valid`).
- Proposal gate: PASS. Design gate: WARN-only/ready for task planning. Pre-implementation tasks gate: PASS; gate output stated “The stage is ready to proceed to implementation.”

## Phase 1: Host and tool contracts

- [x] I1 Add `clankers-engine-host` with async `run_engine_turn(seed, hosts)` over engine-native `EngineRunSeed`, `EngineRunReport`, model execution, tool execution via the `ToolExecutor`/tool-host adapter seam, retry sleep, event sink, cancellation source, and usage observer traits. [covers=embeddable-agent-engine.composable-host-contract,embeddable-agent-engine.host-runner-traits]
- [x] I2 Add `clankers-tool-host` with `ToolCatalog` lookup and metadata listing, `ToolExecutor`, `CapabilityChecker`, `ToolHook`, `ToolOutputAccumulator`, explicit success/error/missing/capability-denied/cancelled/truncated outcomes, and internal UTF-8-safe byte/line truncation metadata. [covers=embeddable-agent-engine.reusable-tool-host,embeddable-agent-engine.tool-host-catalog,embeddable-agent-engine.tool-host-outcome-verification]
- [x] I3 Add `clankers-engine-host::stream` with provider-neutral `HostStreamEvent` and deterministic accumulator outcomes for text, thinking, tool JSON deltas, usage deltas, model name, stop reason, provider errors, and malformed stream input. [covers=embeddable-agent-engine.reusable-stream-accumulator,embeddable-agent-engine.stream-folding-positive,embeddable-agent-engine.stream-folding-negative]

### Phase 1 Evidence Summary

- `cargo test -p clankers-tool-host --lib`: PASS (5 passed, 0 failed).
- `cargo test -p clankers-engine-host --lib`: PASS (16 passed, 0 failed).
- Commits: `f796e986` (`clankers-tool-host`), `43e94d6f` (`clankers-engine-host`), `4b9624c7` (`clankers-engine-host::stream`), `f7834d6f` (runner stream folding).

## Phase 2: Runner and adapter migration

- [x] I4 Move model/tool/retry/cancel feedback construction from `clankers-agent::turn` into `crates/clankers-engine-host/src/runner.rs` and `crates/clankers-engine-host/src/runtime/**`, keeping `SubmitUserPrompt` construction controller-owned. [covers=embeddable-agent-engine.host-feedback-construction-seam,embeddable-agent-engine.cross-reducer-source-rail,embeddable-agent-engine.no-duplicated-runner-policy]
- [x] I5a Route the shared `clankers-agent::Agent` prompt path through the host runner while preserving public `Agent` assembly/API, provider request conversion, event emission, usage updates, model switching, session persistence, capability-gate adapters, and cancellation adapters. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.adapter-rail] [evidence=openspec/changes/extract-composable-engine-host/evidence/i5-agent-host-runner.md]
- [x] I5b Wire standalone interactive, daemon session, and attach prompt entrypoints through the shared `Agent` host-runner seam while preserving hook dispatch and event ordering. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity] [evidence=openspec/changes/extract-composable-engine-host/evidence/i5-agent-host-runner.md]
- [x] I6 Adapt built-in, WASM plugin, and stdio plugin tools through the tool-host executor seam while preserving existing Clankers capability policy through the `CapabilityChecker` seam and without moving plugin supervision, built-in tool bundles, system prompt assembly, daemon protocol, TUI, DB, provider discovery, or model-selection policy into host crates. [covers=embeddable-agent-engine.plugin-tool-adapter,embeddable-agent-engine.host-crate-boundary-rails]
- [x] I7a Preserve core/engine dependency and public API ownership: `clankers-engine` excludes `clankers-core`, core lifecycle types stay out of `clankers-agent`, and `EngineState` keeps an active-field inventory with per-field reducer test or written justification. [covers=embeddable-agent-engine.engine-excludes-core-dependency,embeddable-agent-engine.agent-core-type-rail,embeddable-agent-engine.no-dormant-core-state,embeddable-agent-engine.engine-state-active-data]
- [x] I7b Preserve controller composition seams: `core_effects.rs` normalizes prompt/follow-up effects, `core_engine_composition.rs` builds prompt submission plans, and the composition seam remains `pub(crate)` and testable without daemon/TUI/provider/tool execution. [covers=embeddable-agent-engine.explicit-adapter-composition]
- [x] I7c Preserve adapter-held prompt correlation and cancellation ownership: plans carry `CoreEffectId`, accepted prompt kind, and engine prompt seed outside `EngineState`; terminal outcomes map back to the right `CoreInput`; stale feedback is rejected by `clankers-core`; pre/post-engine cancellation phase ownership follows the host seam. [covers=embeddable-agent-engine.adapter-held-prompt-correlation,embeddable-agent-engine.cancellation-phase-ownership]

### Phase 2 Evidence Summary

- I4 moved non-test `clankers-agent::turn` model/tool/retry/cancel feedback construction to `clankers-engine-host::runtime` helpers while leaving `SubmitUserPrompt` controller/adapter-owned.
- I5a/I5b migrated the shared `run_turn_loop` prompt path to `run_engine_turn` with agent-owned host adapters; standalone, daemon, and attach paths continue to use the shared `Agent` seam.
- `rg 'EngineInput::(ModelCompleted|ModelFailed|RetryReady|ToolCompleted|ToolFailed|CancelTurn)' crates/clankers-agent/src/turn`: non-test feedback constructors no longer appear; remaining matches are test-only.
- `cargo test -p clankers-engine-host --lib && cargo test -p clankers-agent --lib turn::`: PASS (18 host tests, 46 agent turn tests).
- I6 routed existing `Tool` implementations (built-in, WASM plugin, stdio plugin through the shared `Tool` trait) through an agent `ToolExecutor` adapter and added catalog/outcome seam tests; `cargo test -p clankers-agent --lib turn::` PASS (48 tests).
- I7a/I7b/I7c preservation rails stayed green after host-feedback migration: `cargo test -p clankers-controller --test fcis_shell_boundaries` PASS (28 tests).
- Latest migration validation: `cargo test -p clankers-agent --lib turn::` PASS (51 tests), `cargo check -p clankers-agent` PASS, `cargo test -p clankers-controller --test fcis_shell_boundaries` PASS (34 tests).

## Phase 3: Static rails and generated artifacts

- [x] I8a Extend `crates/clankers-controller/tests/fcis_shell_boundaries.rs` FCIS/source rails for `crates/clankers-agent/src/lib.rs` adapter-constant restrictions, all non-test `crates/clankers-agent/src/turn/**/*.rs`, duplicated-runner policy using the full `design.md` Static Rail Design forbidden inventory (`while let Some(effect)`, feedback-driving `for effect in &outcome.effects`, direct feedback constructors, `ScheduleRetry`, pending model/tool fields, retry-budget fields, terminalization helpers, and stop-reason policy branching), and provider-request conversion allowlists. [covers=embeddable-agent-engine.host-extraction-rails,embeddable-agent-engine.no-duplicated-runner-policy,embeddable-agent-engine.adapter-rail] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5a-agent-runner-rails.md]
- [x] I8b Extend `crates/clankers-controller/tests/fcis_shell_boundaries.rs` FCIS/source rails for host runner/runtime feedback constructor allowlists, `SubmitUserPrompt` ownership, `EngineEvent::TurnFinished` direct-match bans, and helper-based terminal observation. [covers=embeddable-agent-engine.host-feedback-construction-seam,embeddable-agent-engine.cross-reducer-source-rail,embeddable-agent-engine.core-engine-boundary-rails]
- [x] I8c Extend `crates/clankers-controller/tests/fcis_shell_boundaries.rs` FCIS/source rails for host crate core-type/API inventories, built-in tool/system-prompt path bans using anchored path-segment matching with bare-word false-positive allowances for `read`, `write`, `diff`, `web`, and `nix`, provider/request/response/timestamp/message-ID leakage, shell-native `AgentMessage` leakage, network/runtime handle leakage (`reqwest::Client`, `hyper::`, `tokio::runtime::Handle`, `tokio::task::JoinHandle`), with exact-token/path-aware matching that allows provider-neutral `HostStreamEvent`, external truncation helper leakage, the `clankers-tool-host` forbidden reducer-internal inventory (`EngineState`, `EngineInput`, `EngineEffect`, `EngineOutcome`, `reduce`, `EngineModelRequest`, `EngineModelResponse`, `EngineToolRequest`, `EngineToolResult`, `RetryReady`, `ScheduleRetry`, `CancelTurn`, terminalization helpers, and model/continuation-budget fields), and matched file/symbol/token/path failure diagnostics. [covers=embeddable-agent-engine.host-crate-boundary-rails,embeddable-agent-engine.core-engine-boundary-rails]
- [x] I8d Extend `crates/clankers-controller/tests/fcis_shell_boundaries.rs` host-crate policy rails so `crates/clankers-engine-host/src/**` rejects retry/backoff constants, continuation-budget mutation/policy, token-limit terminalization, terminalization helper names, direct `StopReason::ToolUse` / `StopReason::MaxTokens` policy branching, and tool-continuation decisions outside `clankers-engine`. [covers=embeddable-agent-engine.host-crate-boundary-rails,embeddable-agent-engine.no-duplicated-runner-policy]
- [x] I9 Extend `scripts/check-llm-contract-boundary.sh` cargo-tree/dependency rails so `clankers-engine`, `clankers-engine-host`, and `clankers-tool-host` reject the full `design.md` Static Rail Design denylist, including `clankers-agent`, `clankers-core`, `clankers-controller`, `clankers-provider`, `clanker-router`, `clankers-db`, `clankers-hooks`, `clankers-plugin`, `clankers-protocol`, `clanker-tui-types`, `clankers-tui`, `ratatui`, `crossterm`, `portable-pty`, `iroh`, `redb`, `reqwest`, `hyper`, `h2`, `tower`, `axum`, `tokio`, `async-std`, `smol`, `actix-rt`, `reqwest-eventsource`, `eventsource-stream`, `chrono`, `time`, `uuid`, `ulid`, `clankers-config`, and `clankers-model-selection`. [covers=embeddable-agent-engine.engine-excludes-core-dependency,embeddable-agent-engine.host-crate-boundary-rails]
- [x] I10 Refresh workspace generated artifacts after adding host crates: workspace manifests, `Cargo.lock`, `flake.nix` test/check crate lists, `build-plan.json` via `unit2nix --workspace --force --no-check -o build-plan.json`, and generated docs via `cargo xtask docs`. [covers=embeddable-agent-engine.host-artifact-freshness,embeddable-agent-engine.host-artifact-refresh]

### Artifact Refresh Evidence Summary

- `unit2nix --workspace --force --no-check -o build-plan.json`: PASS (`Wrote build-plan.json`).
- `cargo xtask docs`: PASS (`docs built → docs/book/`).
- Commit: `f65114d2` refreshed `flake.nix`, `build-plan.json`, and generated docs. Follow-up refresh after adding the agent/adapter dependencies also passed `unit2nix --workspace --force --no-check -o build-plan.json` and `cargo xtask docs`.
- I8a added non-test agent-turn duplicated-runner-policy rails requiring the host-runner seam and rejecting feedback constructors, retry policy, pending model/tool fields, terminalization helper names, direct terminal event matching, and non-test loops over effects; `cargo test -p clankers-controller --test fcis_shell_boundaries` PASS (34 tests).

## Phase 4: Verification

- [x] V1 Add stream accumulator tests for positive text/thinking/tool JSON/usage/model/stop output and negative malformed JSON, non-object JSON, missing starts, duplicate indexes, late deltas, provider errors preserving status and retryability, usage-only deltas, empty stops, plus one parser/adapter seam test feeding provider-native stream data through real normalization into `HostStreamEvent`. [covers=embeddable-agent-engine.reusable-stream-accumulator,embeddable-agent-engine.stream-folding-positive,embeddable-agent-engine.stream-folding-negative] [evidence=openspec/changes/extract-composable-engine-host/evidence/v1-stream-normalization.md]
- [x] V2a1 Add host-runner tests with fake model/sleep/event/cancel/usage adapters for success, failure, retry scheduling with engine-emitted delay/correlation passed to `RetrySleeper`, `RetryReady` emitted only after sleeper completion, and model/retry/terminal single-flight no-concurrency behavior. [covers=embeddable-agent-engine.host-runner-traits] [evidence=openspec/changes/extract-composable-engine-host/evidence/v2-host-runner.md]
- [x] V2a2 Add host-runner tests with fake tool/cancel adapters for sequential tool scheduling, cancellation-before-next-tool, cancellation races against model/tool/retry-sleep futures with exactly one current-state `EngineInput::CancelTurn`, and ignored late results for each phase. [covers=embeddable-agent-engine.host-runner-traits,embeddable-agent-engine.cancellation-phase-ownership] [evidence=openspec/changes/extract-composable-engine-host/evidence/v2-host-runner.md]
- [x] V2a3 Add host-runner tests for terminal outcomes, non-streaming `ModelHost` final-completion-chunk behavior, usage-only and empty-stop streamed results mapping to successful correlated model completion, each malformed stream accumulator error (`MalformedToolJson`, `NonObjectToolJson`, `MissingContentBlockStart`, `DuplicateContentBlockIndex`, `LateContentDelta`) mapping to correlated non-retryable `ModelFailed` with deterministic diagnostics and request ID, and provider-error-to-`ModelFailed` mapping preserving provider status, retryability, and correlated request ID. [covers=embeddable-agent-engine.host-runner-traits] [evidence=openspec/changes/extract-composable-engine-host/evidence/v2-host-runner.md]
- [x] V2b Add host-runner diagnostics/report tests for event-sink failure not altering reducer feedback or terminal behavior, usage deltas delivered in stream arrival order, exactly one final usage summary after model completion, usage-observer failure diagnostics, `EngineRunReport` final state, last outcome, ordered observed events, usage observations, and adapter diagnostics. [covers=embeddable-agent-engine.host-runner-traits,embeddable-agent-engine.tool-host-outcome-verification] [evidence=openspec/changes/extract-composable-engine-host/evidence/v2-host-runner.md]
- [x] V2c Add host-runner negative feedback and tool-outcome mapping tests for wrong-phase, duplicate, mismatched, post-terminal feedback, ignored late adapter results, and `Succeeded`, `Truncated`, `ToolError`, `MissingTool`, `CapabilityDenied`, and `Cancelled` tool-host outcomes into correlated engine feedback with cancellation precedence. [covers=embeddable-agent-engine.host-runner-traits,embeddable-agent-engine.tool-host-outcome-verification] [evidence=openspec/changes/extract-composable-engine-host/evidence/v2-host-runner.md]
- [x] V3a Add tool-host tests for catalog lookup, metadata listing, success, tool error, missing tool, capability denial, tool cancellation, result accumulation, and hook ordering. [covers=embeddable-agent-engine.reusable-tool-host,embeddable-agent-engine.tool-host-catalog,embeddable-agent-engine.tool-host-outcome-verification] [evidence=openspec/changes/extract-composable-engine-host/evidence/v3-tool-host.md]
- [x] V3b Add tool truncation tests for adapter-supplied truncation limits, UTF-8 boundary truncation, newline-boundary line counting, original/truncated byte and line counts, deterministic metadata, and existing Clankers tool-output limit parity. [covers=embeddable-agent-engine.tool-host-catalog,embeddable-agent-engine.tool-host-outcome-verification] [evidence=openspec/changes/extract-composable-engine-host/evidence/v3b-truncation.md]
- [x] V3c Add built-in/WASM/stdio adapter wrapper tests proving all tool kinds share the tool-host executor seam and plugin supervision does not move into generic host crates. [covers=embeddable-agent-engine.plugin-tool-adapter,embeddable-agent-engine.host-crate-boundary-rails] [evidence=openspec/changes/extract-composable-engine-host/evidence/v3c-mixed-tool-adapter.md]
- [x] V4a1 Add shared `Agent` seam parity tests for streaming deltas, non-streaming final-completion ordering, tool events/failures, event ordering, and sequential tool scheduling through the host runner. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4-agent-host-runner-parity.md]
- [x] V4a2 Add shared `Agent` seam parity tests for usage updates/final summary, hook dispatch, capability-gate behavior, and model switching through the host runner. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4-agent-host-runner-parity.md]
- [x] V4a3 Add shared `Agent` seam parity tests for shell-visible retry/backoff behavior, cancellation-before-next-tool behavior, shell-visible budget exhaustion, zero-budget rejection, token-limit terminalization, and terminal behavior through the host runner. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity,embeddable-agent-engine.adapter-rail] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4-agent-host-runner-parity.md]
- [x] V4b1 Add standalone interactive smoke/parity tests proving prompts route through the shared host-runner seam. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4-entrypoint-routing.md]
- [x] V4b2 Add daemon session and attach smoke/parity tests proving prompts route through the shared host-runner seam. [covers=embeddable-agent-engine.agent-default-assembly,embeddable-agent-engine.host-adapter-parity] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4-entrypoint-routing.md]
- [x] V4c Add engine reducer tests for retryable failure scheduling, default 1-second/4-second retry delays, retry exhaustion, preserved correlation IDs, initial request budget counting, continuation budget, budget exhaustion, zero-budget rejection, `StopReason::MaxTokens` terminalization, and deterministic terminal effect ordering. [covers=embeddable-agent-engine.reducer-retry-tests,embeddable-agent-engine.reducer-budget-token-tests] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4cd-engine-reducer.md]
- [x] V4d Add invalid feedback reducer tests for mismatched request IDs, wrong-phase retry/model/tool feedback, duplicate feedback, invalid retry after terminalization, and post-terminal feedback leaving state unchanged with no effects. [covers=embeddable-agent-engine.invalid-retry-feedback] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4cd-engine-reducer.md]
- [x] V4e1 Add adapter composition positive tests for core prompt acceptance/start, adapter engine submission, host-runner turn execution, core completion feedback, and post-prompt follow-up evaluation. [covers=embeddable-agent-engine.composition-tests] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4e-composition.md]
- [x] V4e2 Add adapter composition positive tests for shell-visible prompt lifecycle, loop, auto-test, thinking, disabled-tool, retry, cancellation, and terminal behavior. [covers=embeddable-agent-engine.composition-tests] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4e-composition.md]
- [x] V4e3 Add adapter composition negative tests for out-of-order completion, mismatched effect IDs, wrong-phase engine feedback, and attempted lifecycle/turn feedback to the wrong reducer. [covers=embeddable-agent-engine.composition-tests] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4e-composition.md]
- [x] V4f Add controller composition seam tests proving `core_effects.rs` normalizes prompt/follow-up effects, `core_engine_composition.rs` builds prompt submission plans, the seam remains `pub(crate)` and testable without daemon/TUI/provider/tool execution, plans carry `CoreEffectId`, accepted prompt kind, and engine prompt seed outside `EngineState`, terminal outcomes map to the right `CoreInput`, and stale feedback is rejected by `clankers-core`. [covers=embeddable-agent-engine.explicit-adapter-composition,embeddable-agent-engine.adapter-held-prompt-correlation] [evidence=openspec/changes/extract-composable-engine-host/evidence/v4f-controller-composition.md]
- [x] V5a Add source rail tests proving no duplicated runner policy in `clankers-agent::turn` using the full `design.md` Static Rail Design forbidden-token inventory, `crates/clankers-agent/src/lib.rs` adapter-constant restrictions, feedback-constructor allowlists, and terminal-observation helper rules in both `clankers-agent::turn` and `clankers-engine-host`, and failing rails print matched file plus symbol/token/path. [covers=embeddable-agent-engine.host-extraction-rails,embeddable-agent-engine.no-duplicated-runner-policy,embeddable-agent-engine.host-feedback-construction-seam,embeddable-agent-engine.cross-reducer-source-rail] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5a-agent-runner-rails.md]
- [x] V5b Add host crate dependency/API leakage rails proving the full `design.md` Static Rail Design forbidden normal dependency denylist, provider/request/response/timestamp/message-ID public contracts, shell-native `AgentMessage`, and network/runtime handles with exact-token/path-aware matching that allows provider-neutral `HostStreamEvent`, built-in tool/system-prompt paths using anchored path-segment matching and bare-word false-positive tests, external truncation helpers, and the explicit `clankers-tool-host` forbidden reducer-internal token inventory are rejected, with failing rails printing matched file plus symbol/token/path. [covers=embeddable-agent-engine.host-crate-boundary-rails] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5b-boundary-rails.md]
- [x] V5e Add host-crate policy source rail tests proving `clankers-engine-host` rejects retry/backoff constants, continuation-budget policy, token-limit terminalization, terminalization helper names, `StopReason::ToolUse` / `StopReason::MaxTokens` policy branching, and tool-continuation decisions, with failing rails printing matched file plus symbol/token/path. [covers=embeddable-agent-engine.host-crate-boundary-rails,embeddable-agent-engine.no-duplicated-runner-policy] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5-host-policy-rails.md]
- [x] V5c Add core/engine ownership rails proving no core/engine ownership drift, the explicit `agent-core-type-rail` forbidden-token inventory over non-test `crates/clankers-agent/src/**`, and that `EngineState` inventory remains active with one reducer test or written justification per remaining field, with failing rails printing matched file plus symbol/token/path. [covers=embeddable-agent-engine.core-engine-boundary-rails,embeddable-agent-engine.engine-state-active-data,embeddable-agent-engine.agent-core-type-rail] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5c-core-engine-ownership.md]
- [x] V5d Add artifact freshness checks proving generated artifacts contain the new host crates. [covers=embeddable-agent-engine.host-artifact-refresh,embeddable-agent-engine.host-artifact-freshness] [evidence=openspec/changes/extract-composable-engine-host/evidence/v5d-artifact-freshness.md]
- [ ] V6 Run final validation bundle and record evidence: prerequisite commands from R1, focused crate tests, FCIS/source rails, cargo-tree rails, artifact freshness checks, `cargo check`/`cargo test` coverage for new crates and migrated adapters, and `openspec validate extract-composable-engine-host --strict`. [covers=embeddable-agent-engine.composable-host-contract,embeddable-agent-engine.reusable-tool-host,embeddable-agent-engine.reusable-stream-accumulator,embeddable-agent-engine.host-extraction-rails,embeddable-agent-engine.host-artifact-freshness] [evidence=openspec/changes/extract-composable-engine-host/evidence/validation-plan.md]
