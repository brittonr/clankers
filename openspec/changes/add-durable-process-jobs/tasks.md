## Phase 1: Storage and contract foundation

- [x] [serial] [covers=durable-process-jobs.interfaces.tool-service-boundary] Define backend-neutral `ProcessJobService`, `ProcessJobBackend`, store, log store, notification sink, and projection traits/DTOs before wiring concrete backends. ✅ completed: 2026-05-17T05:39:45Z; evidence: `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers-runtime --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [serial] [covers=durable-process-jobs.redb.metadata-log-reference] Define process/job record DTOs, status vocabulary, redaction rules, and redb table schema in `clankers-db`; include migration a...[truncated]
- [x] [parallel] [covers=durable-process-jobs.receipts.typed] Define structured receipt/error DTOs for start/list/poll/log/wait/kill/restart/stdin/adopt/GC with stable id, backend kind, typed status, log refs, and unsupported-action codes. ✅ completed: 2026-05-17T05:50:16Z; evidence: `ProcessJobToolResult` covers start/list/poll/log/wait/kill/restart/stdin/adopt/GC, `ProcessJobReceipt::unsupported(...)` pins unsupported-action code, `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers-runtime --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.identity-ownership.stable-id] Define stable Clankers id, backend reference, owner/session/workspace scope, and cross-session capability model. ✅ completed: 2026-05-17T05:51:47Z; evidence: `ProcessJobId`, `BackendRef`, `ProcessJobOwnerScope`, `ProcessJobCallerScope`, `ProcessJobCapabilitySet`; tests `observe_only_scope_denies_cross_session_mutation` and `durable_backend_and_stdin_require_explicit_capabilities`; `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers-runtime --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.logs.bounded] Define bounded log reference/chunk/cursor DTOs and native append-only log file retention behavior without storing unbounded output in redb. ✅ completed: 2026-05-17T05:53:14Z; evidence: `ProcessJobLogRef`, `ProcessJobLogCursor`, `ProcessJobLogRange`, `ProcessJobLogChunk`, `NativeProcessJobLogLayout`, `ProcessJobLogRetentionPolicy`; tests `native_log_layout_is_append_only_bounded_and_safe` and `log_chunks_carry_cursor_and_truncation_explicitly`; `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers-runtime --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.backends.native-compat] Inventory current `process` tool semantics and write compatibility expectations for shell/direct start, poll/log/wait/kill/stdin/close, and dangerous-command policy. ✅ completed: 2026-05-17T05:53:43Z; evidence: `openspec/changes/add-durable-process-jobs/native-process-compat.md` inventories `src/tools/process.rs` schema/actions, native shell/direct start, dangerous command blocking, poll/log/wait/kill/write/submit/close behavior, status mapping, and durable-backend compatibility constraints.
- [x] [serial] [depends:durable-process-jobs.interfaces.tool-service-boundary] Add fake store/backend/log/notification sink contract tests that prove tool/service/backend/storage/projection boundaries remain decoupled. ✅ completed: 2026-05-17T05:56:21Z; evidence: runtime test `fake_boundaries_compose_without_concrete_coupling` composes `ProcessJobBackend`, `ProcessJobStore`, `ProcessJobLogStore`, `ProcessJobNotificationSink`, and `ProcessJobProjection` trait objects without concrete storage/backend coupling; `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers-runtime --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [serial] [depends:durable-process-jobs.redb.metadata-log-reference] Add a storage facade that wraps redb access through `Db::blocking(...)` for async tool/controller paths. ✅ completed: 2026-05-17T05:59:58Z; evidence: `AsyncProcessJobStore` exposes async `upsert/get/list/count` via `Db::blocking(...)`; `Db::async_process_jobs()` facade accessor; test `async_facade_uses_blocking_store_roundtrip`; `cargo test -p clankers-db process_jobs -- --nocapture`, `cargo check -p clankers-db --tests`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.

## Phase 2: Native durable backend

- [x] [serial] [depends:phase-1] Extract a backend-neutral process/job service and backend trait boundary while keeping the current native backend behavior as the default path. ✅ completed: 2026-05-17T06:06:26Z; evidence: `NativeProcessJobService` implements backend-neutral `ProcessJobService` over the existing native process registry/spawn/wait/kill/stdin behavior; current `ProcessTool` native path still passes compatibility tests; test `native_process_job_service_preserves_default_start_list_wait_flow`; `cargo test -p clankers --lib tools::process::tests::native_process_job_service_preserves_default_start_list_wait_flow -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [serial] [covers=durable-process-jobs.registry.finished-history] Persist native process start/status/completion metadata and log references; make `process list/log/poll` use durable state where available. ✅ completed: 2026-05-17T06:12:16Z; evidence: `ProcessTool` persists native start/timeout/completion metadata through `ToolContext::db().async_process_jobs()`, waiter records terminal state, durable-only records appear in `list`, and `poll/log/wait` return durable metadata/log references when no live registry entry is available; test `process_tool_persists_native_metadata_and_uses_durable_fallback`; `cargo test -p clankers --lib tools::process::tests::process_tool_persists_native_metadata_and_uses_durable_fallback -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [serial] [covers=durable-process-jobs.registry.restart-reattach] Implement daemon-start reconciliation for native process records, including running/reattached, `reattached-log-incomplete`, exited, and `lost-after-restart` outcomes. ✅ completed: 2026-05-17T06:17:50Z; evidence: `build_agent_with_tools` schedules durable native reconciliation when a DB is attached and process durable read/list uses the same reconciler; live PIDs with log refs remain running with `reconciliation=reattached`, live PIDs without log refs become `reattached-log-incomplete`, terminal records are preserved as exited, and active records without a live PID become `lost-after-restart`; test `process_list_reconciles_durable_native_restart_states`; `cargo test -p clankers --lib tools::process::tests::process_list_reconciles_durable_native_restart_states -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.admission.native-limit] Enforce native concurrency/admission limits before spawn, returning typed reject/queue receipts without silently exceeding policy. ✅ completed: 2026-05-17T14:43:12Z; evidence: native starts reserve admission capacity before spawn and reject at the configured active/reserved limit with a typed `concurrency_limit_exceeded` start receipt instead of silently exceeding policy; tests `native_admission_limit_rejects_at_capacity_with_typed_receipt` and `native_admission_reservations_count_against_capacity_before_spawn`; `cargo test -p clankers --lib tools::process::tests::native_admission -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.lifecycle.detach] Ensure agent cancellation, client detach, log-follow stop, and notification unsubscribe do not kill a job unless explicit kill/cancel is authorized. ✅ completed: 2026-05-17T14:46:02Z; evidence: `process` start hands child ownership to registry reader/waiter tasks rather than the caller context, and cancellation/drop of a wait/log-following caller leaves the native job running/listable until completion or explicit kill; test `cancellation_detach_and_log_poll_do_not_kill_process`; `cargo test -p clankers --lib tools::process::tests::cancellation_detach_and_log_poll_do_not_kill_process -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.lifecycle.kill-escalation] Implement explicit graceful-kill/escalation semantics and typed terminal outcomes for native process groups. ✅ completed: 2026-05-17T14:50:21Z; evidence: native process-group kill now records explicit `graceful-term`, `escalated-sigkill`, or `direct-kill` terminal outcomes and uses SIGTERM grace before SIGKILL escalation; tests `kill_stops_running_process`, `kill_escalates_process_group_after_grace_period`, and `kill_stops_shell_descendants`; `cargo test -p clankers --lib process:: -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.capabilities.observe-only] Add capability checks that permit observe/log separately from start/kill/restart/stdin mutation. ✅ completed: 2026-05-17T16:23:24Z; evidence: UCAN process gate now permits action-scoped `process:observe` + `process:logs` for list/poll/wait/log without granting start/kill/restart/stdin mutation, while preserving legacy `process` full-tool authorization; tests `process_observe_and_log_caps_do_not_allow_mutation`, `process_log_requires_observe_and_log_caps`, and `legacy_process_tool_cap_allows_process_actions`; `cargo test -p clankers --lib capability_gate -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture`, `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [parallel] [covers=durable-process-jobs.capabilities.backend-stdin] Add explicit authorization checks for stdin and non-native backend selection before any backend mutation occurs. ✅ completed: 2026-05-17T16:33:29Z; evidence: UCAN process gate requires `process:mutate` + `process:stdin` for write/submit/close, requires `process:backend` before selecting any non-native backend such as pueue/systemd/unknown backend strings, and still allows native starts with `process:start`; tests `process_stdin_requires_mutate_and_stdin_caps` and `non_native_backend_selection_requires_backend_cap`; `cargo test -p clankers --lib capability_gate -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture --test-threads=1` (parallel full process suite timed out after all but two process tests passed; reran those targets individually and the serialized suite passed), `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.

## Phase 3: Durable backend projections

- [x] [serial] [depends:phase-2] Add fake backend contract tests for list/poll/log/kill/restart/status projection, backend capability matrix, and unsupported-backend/action receipts. ✅ completed: 2026-05-17T16:36:48Z; evidence: runtime fake backend tests now cover start/list/status-observe/log/kill/restart projection through shared backend/store/projection traits, explicit backend capability matrix assertions, and typed backend-unavailable + unsupported-action receipts; tests `fake_backend_contract_covers_projection_and_mutations`, `fake_backend_capability_matrix_and_unavailable_receipts_are_explicit`; `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [x] [serial] [covers=durable-process-jobs.backends.pueue] Implement pueue backend integration behind availability/config checks, projecting pueue task ids, statuses, logs, groups, and restarts into Clankers receipts. ✅ completed: 2026-05-17T17:12:55Z; evidence: `PueueRunner` trait + `PueueProcessJobService` keep the pueue CLI behind an injectable runner seam (not hard-seamed to the tool); process tool exposes `backend: "pueue"` routing for start/list/poll/log/wait/kill/restart and typed unsupported stdin receipts; start uses availability/config guard before `pueue add`, projects pueue ids as `pueue_<id>` with `pueue:<id>` backend refs, group/label metadata, status JSON, log JSON, kill, and in-place restart; deterministic fake-runner tests cover projection/mutations, unavailable-before-mutation, and disabled config guard; checks `cargo test -p clankers --lib pueue_backend -- --nocapture`, `cargo test -p clankers --lib process:: -- --nocapture --test-threads=1`, `cargo test -p clankers-runtime process_jobs -- --nocapture`, `cargo check -p clankers --tests`, `cargo fmt --check`, `openspec validate add-durable-process-jobs --strict --json`, `git diff --check`.
- [ ] [serial] [covers=durable-process-jobs.backends.systemd] Implement systemd backend integration behind availability/config checks, projecting transient units/scopes, cgroup kill/restart, and journal/log references into Clankers receipts.
- [ ] [parallel] [covers=durable-process-jobs.notifications-delivery.reattach] Persist notification events through the notification sink so detached sessions can receive completion/readiness state on authorized reattach.
- [ ] [parallel] [covers=durable-process-jobs.logs.notifications] Add `notify_on_complete` and bounded `watch_patterns` support with rate limits, one-shot completion delivery, rare readiness-signal delivery, and deterministic tests for noisy pattern suppression.
- [ ] [parallel] [covers=durable-process-jobs.adoption.identity-check] Add optional adoption/import service methods for PID, pueue task id, and systemd unit name with fail-closed identity/capability checks.

## Phase 4: NixOS and TUI integration

- [ ] [serial] [covers=nixos-process-job-config.module.persistence] Extend `nix/modules/clankers-daemon.nix` with process/job persistence options, directory/log configuration, database path wiring, retention/GC policy, and hardening write-path updates.
- [ ] [serial] [covers=nixos-process-job-config.module.pueue] Add optional pueue service/config integration and deterministic module eval tests for groups/concurrency and disabled fallback behavior.
- [ ] [serial] [covers=nixos-process-job-config.module.systemd-limits] Add systemd backend resource-limit options and validation/eval tests for memory, CPU, runtime, writable paths, and kill grace period.
- [ ] [parallel] [covers=durable-process-jobs.project-profiles.resolve] Add validated project job profile parsing that resolves named profiles into backend-neutral `ProcessJobSpec` values without backend-specific execution from config code.
- [ ] [parallel] [covers=durable-process-jobs.receipts.projection] Update process/job TUI/procmon/daemon projection data so native, pueue, and systemd backends appear in one bounded active/completed view with backend labels and shared DTOs.
- [ ] [parallel] [covers=durable-process-jobs.retention.completed-gc] Implement retention/GC command and automatic policy enforcement with typed receipts for removed metadata/logs, skipped active jobs, and failures.

## Phase 5: Verification and rollout

- [ ] [serial] [depends:phase-4] Add restart/reconciliation integration coverage using a temp redb/log dir and a long-lived test child; verify stable id, log reference, and honest lost/reattached status.
- [ ] [serial] [depends:phase-4] Add detach/reattach notification delivery tests covering persisted completion events, multi-client event ids, and deduplication behavior.
- [ ] [serial] [depends:phase-4] Add or refresh NixOS VM smoke coverage for process persistence enabled, daemon restart, list/log status, retention paths, and service directory/hardening behavior where practical.
- [ ] [parallel] [depends:phase-4] Update README/help text for durable `process` backend selection, typed receipts/errors, pueue/systemd availability errors, retention/GC, project job profiles, and NixOS module options.
- [ ] [serial] [depends:phase-5] Run focused checks: redb store tests, service-boundary fake tests, process tool/backend tests, capability/admission tests, notification sink tests, Nix module eval tests, and TUI/procmon DTO tests.
- [ ] [serial] [depends:phase-5] Run final gates: `cargo fmt --check`, `cargo nextest run` or focused workspace test set agreed during implementation, relevant NixOS module/VM checks, `openspec validate add-durable-process-jobs --strict --json`, and `git diff --check`.
