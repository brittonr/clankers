## 1. Provider-aware auth plumbing

- [x] 1.1 Add a provider-aware OAuth driver abstraction in the shared auth layer so Anthropic and `openai-codex` login flows share the same command and slash-command plumbing
- [x] 1.2 Implement OpenAI Codex authorize, code-exchange, load/reload, refresh, account-ID derivation, and malformed/missing-claim rejection helpers, keep `chatgpt_account_id` derived from the active token instead of adding a persisted auth-store field, then store credentials under provider `openai-codex` and mark the requested `openai-codex` account active
- [x] 1.3 Isolate pending OAuth verifier and state storage by provider plus requested clankers account name from the existing `--account <name>` / slash-account syntax so Anthropic and `openai-codex` in-flight logins, or two `openai-codex` logins for different accounts, cannot overwrite each other
- [ ] 1.4 Wire `openai-codex` into the proactive/background refresh path so refreshed credentials update the persisted auth store before expiry and reset in-memory entitlement state for re-evaluation without adding a persisted entitlement field
- [x] 1.5 Update `clankers auth` and `/login` / `/account` handlers to accept explicit provider names, implement provider-scoped switch/logout behavior, show grouped and provider-scoped status including provider-specific OAuth validity/expiry, preserve omitted-provider Anthropic defaults, reload the affected provider after a successful login, and invalidate/recompute entitlement state on provider reload or account switch
- [x] 1.6a Add tests for provider-aware auth parsing, pending-state isolation, and omitted-provider Anthropic defaults across CLI and slash-command login, status, switch, and logout flows
- [x] 1.6b Add tests for grouped and provider-scoped status output, separate OAuth validity/expiry reporting, provider-scoped switch/logout, and persistence while preserving existing `anthropic` / API-key `openai` credentials after `openai-codex` login
- [ ] 1.6c Add tests for proactive/background refresh plus provider reload/account switch entitlement recomputation
- [ ] 1.6d Add tests for token refresh, malformed/missing-claim rejection, request blocking without a valid account ID, account-ID derivation from loaded and refreshed credentials, and activation of the requested `openai-codex` account after login
- [x] 1.7 Add a deterministic auth-fixture check for the OpenAI OAuth contract: authorize URL base + required query parameters, token endpoint URL, and required exchange/refresh form fields

## 2. Request metadata plumbing

- [x] 2.1 Add `extra_params` to `crates/clankers-provider::CompletionRequest`, then audit every router-bound `CompletionRequest { ... }` construction path in `crates/clankers-agent/src/turn/execution.rs`, `src/modes/agent_task.rs`, `src/worktree/llm_resolver.rs`, `crates/clankers-provider/src/router.rs`, `crates/clankers-provider/src/rpc_provider.rs`, and router test/helper constructors so provider extras are preserved or explicitly initialized
- [x] 2.2 Pass `extra_params` through `RouterCompatAdapter` and `RpcProvider`, and thread the literal persisted clankers session identifier from interactive and agent entry points into request `extra_params` as `_session_id`
- [x] 2.3 Add tests that the `_session_id` key inside provider-specific `extra_params` survives both local and RPC provider paths, is reused across later turns, and is reused again after session resume
- [x] 2.4 Add a deterministic constructor-inventory check covering router-bound `CompletionRequest` construction in `crates/clankers-agent/src/turn/execution.rs`, `src/modes/agent_task.rs`, `src/worktree/llm_resolver.rs`, `crates/clankers-provider/src/router.rs`, `crates/clankers-provider/src/rpc_provider.rs`, and router test/helper constructors so new call paths cannot omit `extra_params` silently
- [x] 2.5 Add a deterministic cross-repo schema/serialization parity check covering `crates/clankers-provider::CompletionRequest` and `clanker-router::CompletionRequest` so `extra_params`, including the `_session_id` key inside that map, survive local and RPC transport boundaries unchanged

## 3. OpenAI Codex backend and discovery

- [ ] 3.1 Add curated `openai-codex` model catalog entries, exact-set verification for the six initial model IDs and no extras, entitlement evaluation/state, and discovery/status logic that only exposes the provider when an `openai-codex` account is both authenticated and entitled, triggers probing from discovery plus grouped/provider-scoped status when entitlement is unknown, classifies HTTP 403 and structured error code `usage_not_included` as not entitled, surfaces authenticated-but-not-entitled or entitlement-check-failed state for unsupported accounts, and keeps entitlement state in memory only
- [ ] 3.2 Implement a dedicated OpenAI Codex backend in `clanker-router` for the ChatGPT Codex Responses SSE transport, including request conversion for current-turn user input, ordered prior user-turn replay, prior assistant text/refusal items, `instructions` = system prompt only, original tool call/item IDs, paired `function_call_output` items, auth-layer-provided account-ID header injection, auth headers, `reasoning.encrypted_content`, `text={"verbosity":"medium"}` by default with caller override support, entitlement probes against `POST https://chatgpt.com/backend-api/codex/responses`, and session cache key handling
- [ ] 3.3 On explicit or resumed `openai-codex` requests, trigger the entitlement probe first when state is `unknown`, send the normal Codex turn only after a successful probe, fail closed with an explicit ChatGPT Plus/Pro-required message when the account is `not_entitled`, fail closed with a retriable provider error plus entitlement-check-failed semantics when the probe cannot classify the account, and never fall back to API-key `openai`
- [ ] 3.4 Map raw Codex SSE events `response.output_item.added`, `response.reasoning_summary_part.added`, `response.reasoning_summary_text.delta`, `response.reasoning_summary_part.done`, `response.content_part.added`, `response.output_text.delta`, `response.refusal.delta`, `response.function_call_arguments.delta`, `response.function_call_arguments.done`, `response.output_item.done`, and `response.completed` into generic `StreamEvent` boundaries, including exactly-once `MessageStart`, `ContentBlockStart`, `ContentBlockDelta`, `ContentBlockStop`, `MessageDelta`, and `MessageStop` for text, thinking, and tool-use flows; ensure `response.reasoning_summary_part.added/done` do not create extra generic `ContentBlockStart/Stop` boundaries; map `completed` to a normal stop, `incomplete` to the generic incomplete/max-tokens stop path, treat `failed` and `cancelled` as provider errors instead of successful `MessageStop`, and suppress `queued` and `in_progress` as final stop reasons; carry tool-call correlation as `<call_id>|<item.id>` plus tool name; replay prior-turn reasoning from `Content::Thinking.signature`; and exclude display-only thinking text from replay payloads
- [ ] 3.5 Implement the chosen retry envelope for Codex requests: up to 3 transient retries on 429 or retryable 5xx with 1s/2s/4s backoff, plus exactly one reactive 401 refresh-and-retry, continue with the remaining transient budget after refresh, and never perform more than one refresh cycle per top-level request
- [ ] 3.6 Add tests for discovery and model resolution with entitled, missing, and unsupported `openai-codex` accounts, including user-visible provider-qualified catalog entries, authenticated-but-not-entitled and entitlement-check-failed status output, status-triggered probing when entitlement is unknown, explicit/resumed request blocking for both unsupported-account and entitlement-check-failed paths, while API-key `openai` behavior stays unchanged
- [ ] 3.7a Add fixture-backed unit and integration tests for normal request mapping: first-turn current-user input, ordered prior user-turn replay, assistant text/refusal replay, original tool call/item IDs, paired `function_call_output` items, reasoning signatures, signature replay without resending display-only thinking text, and session cache key propagation
- [ ] 3.7b Add fixture-backed unit and integration tests for Codex stream normalization: exact generic stream boundaries (`MessageStart`, `ContentBlockStart/Delta/Stop`, `MessageDelta`, `MessageStop`), raw SSE event handling for `response.output_item.added` / `response.reasoning_summary_part.added` / `response.reasoning_summary_text.delta` / `response.reasoning_summary_part.done` / `response.content_part.added` / `response.output_text.delta` / `response.refusal.delta` / `response.function_call_arguments.delta` / `response.function_call_arguments.done` / `response.output_item.done` / `response.completed -> MessageDelta + MessageStop`, explicit verification that `response.reasoning_summary_part.added/done` do not create extra generic `ContentBlockStart/Stop` boundaries, tool-call correlation, usage normalization, and stop-reason mapping including `incomplete` -> generic incomplete/max-tokens stop plus `failed`/`cancelled` provider-error behavior and suppression of `queued`/`in_progress` as final stop reasons
- [ ] 3.7c Add fixture-backed retry and entitlement-classification tests covering direct no-retry behavior for non-401 4xx responses, retry-envelope behavior including exact `1s/2s/4s` no-jitter delays plus combined 401+transient sequences and remaining-budget checks, classification of HTTP 403 and `usage_not_included` as not entitled, and unknown-entitlement handling after probe failure
- [ ] 3.7d Add at least one real raw-SSE runtime seam test at the actual parser entrypoint rather than only helper-level wrappers
- [ ] 3.8 Add a deterministic request-fixture check that normal Codex requests send required headers (`Authorization`, `chatgpt-account-id`, `OpenAI-Beta`, `originator`, `accept`, `content-type`, and `session_id` when applicable) on initial request, transient retry, and 401 refresh-retry paths
- [ ] 3.9 Add a deterministic normal-request body fixture check for `POST https://chatgpt.com/backend-api/codex/responses` on initial, transient-retry, and 401 refresh-retry paths: required `model`, `store=false`, `stream=true`, `instructions` containing only the system prompt, ordered `input` replay for prior user turns and assistant text/refusal turns, `text={"verbosity":"medium"}` by default with caller override verification, `include=["reasoning.encrypted_content"]`, `tool_choice="auto"`, `parallel_tool_calls=true`, optional `tools`/`reasoning`, and `prompt_cache_key` when `_session_id` is present
- [ ] 3.10 Add deterministic entitlement-probe fixture checks for initial, transient-retry, and 401 refresh-retry probe paths: `POST https://chatgpt.com/backend-api/codex/responses`, required auth/account headers plus `OpenAI-Beta: responses=experimental`, `originator`, and `content-type`, explicit omission of `accept: text/event-stream` and `session_id`, `model=gpt-5.1-codex-mini`, `store=false`, `stream=false`, minimal `instructions`/`input`/`text`, no tools, no prompt-cache key, and no session-specific state
- [ ] 3.11 Add a deterministic regression check that API-key `openai` keeps its existing transport and omits Codex-only headers and session semantics
- [ ] 3.12 Update the extracted `clanker-router` dependency or pin in this workspace after the backend and auth changes land there, including any required `crate-hashes.json` and `flake.nix` source updates for Nix builds

## 4. UX, docs, and verification

- [ ] 4.1 Update CLI help, slash help, provider lists, and model-selection docs to describe `openai-codex` login, account naming via existing `--account` / slash-account syntax, personal-use/plan limitations, unsupported-plan behavior, and the separation from API-key `openai`
- [ ] 4.2 Add a docs/help acceptance or snapshot check proving CLI help, slash help, and provider docs cover `openai-codex` login, account naming, model selection, personal-use/plan limitations, unsupported-plan behavior, and unchanged API-key `openai` help paths
- [ ] 4.3 Add or update recorded-fixture end-to-end coverage for login, status/account switching, interactive credential reload, model resolution, and one Codex turn where live credentials are not available in CI
- [ ] 4.4 Run `cargo nextest run`, `cargo clippy -- -D warnings`, and `nix build .#clankers` after the workspace and extracted-crate updates are in place
- [ ] 4.5 After fixture and automated verification pass, run one manual live-credential smoke path for login, status/account switching, interactive credential reload, model resolution, and one Codex turn against a real ChatGPT Plus/Pro subscription
- [ ] 4.6 Record the manual live-credential smoke result in the implementation PR or change notes before the change is marked ready
