## 0. OpenSpec readiness

- [x] 0.1 Before implementation starts, rerun `openspec validate engine-retry-stop-policy --strict` and always capture the output in `openspec/changes/engine-retry-stop-policy/evidence/preimplementation-validation.md` with evidence fields: `Artifact-Type`, `Evidence-ID`, `Task-ID`, `Covers`, `Creator`, `Created`, `Status`, `Command`, and `Output`. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.adapter-parity-rails,turn-level-retry.engine-authoritative,turn-level-retry.no-duplicate-messages,turn-level-retry.cancellation-during-backoff] [evidence=openspec/changes/engine-retry-stop-policy/evidence/preimplementation-validation.md]
  Evidence summary: `openspec/changes/engine-retry-stop-policy/evidence/preimplementation-validation.md` has `Artifact-Type: verification-log`, `Evidence-ID: engine-retry-stop-policy.preimplementation-validation`, `Task-ID: 0.1`, matching `Covers`, `Creator: pi`, `Created: 2026-04-24T16:44:00Z`, `Status: PASS`, command `openspec validate engine-retry-stop-policy --strict`, and output `Change 'engine-retry-stop-policy' is valid`.

## 1. Reducer tests first

- [ ] 1.1 Add failing `clankers-engine` reducer tests for retry scheduling through `EngineEffect::ScheduleRetry { request_id, delay }`, preserving the same pending model request ID, retry-ready re-emitting `RequestModel`, and default 1-second / 4-second / no-jitter delays. [covers=embeddable-agent-engine.retry-scheduling,embeddable-agent-engine.retry-defaults,embeddable-agent-engine.reducer-retry-tests,turn-level-retry.engine-authoritative,turn-level-retry.transient-retry]
- [ ] 1.2 Add failing `clankers-engine` reducer tests for retry attempt accounting, successful model feedback clearing the retry counter, and follow-up model requests receiving a fresh retry budget. [covers=embeddable-agent-engine.retry-budget-reset,embeddable-agent-engine.reducer-retry-tests,turn-level-retry.retry-budget-scope]
- [ ] 1.3 Add failing `clankers-engine` reducer tests for retry exhaustion and non-retryable model failure terminalization with `BusyChanged(false)`, `EngineTerminalFailure { message, status, retryable }` populated from the latest host-supplied failure details, failure notice, and `TurnFinished(Stop)` ordering, plus no `ScheduleRetry`, no `RequestModel`, and no canonical message mutation on those terminal failure paths. [covers=embeddable-agent-engine.non-retryable-terminalization,embeddable-agent-engine.retry-exhaustion-terminalization,embeddable-agent-engine.reducer-retry-tests,turn-level-retry.retry-exhaustion,turn-level-retry.non-retryable]
- [ ] 1.4 Add failing `clankers-engine` reducer tests for cancellation while retry is scheduled, asserting `CancelTurn { reason: "turn cancelled" }` clears pending model work, emits `BusyChanged(false)`, `Notice { message: "turn cancelled" }`, then `TurnFinished(Stop)`, emits no `RequestModel`, and causes later retry-ready/model-success/model-failure feedback for the cancelled request to return `InvalidPhase` with unchanged state and no effects. [covers=embeddable-agent-engine.retry-wait-cancellation,turn-level-retry.cancellation-during-backoff,turn-level-retry.cancelled-retry-no-attempt]
- [ ] 1.5 Add failing `clankers-engine` reducer tests proving failed retry attempts do not mutate canonical messages. [covers=embeddable-agent-engine.reducer-retry-tests,turn-level-retry.no-duplicate-messages,turn-level-retry.failed-attempt-no-message-mutation]
- [ ] 1.6 Add failing `clankers-engine` reducer tests for model-continuation budget counting: initial request consumes one slot, within-budget follow-up tool feedback emits a follow-up `RequestModel`, follow-up requests consume slots, retries do not consume slots, and budget exhaustion retains accepted tool feedback, clears pending tool work, emits `BusyChanged(false)` / `Notice { message: "engine model request slot budget exhausted" }` / `TurnFinished(Stop)`, leaves `terminal_failure: None`, and emits no `RequestModel`. [covers=embeddable-agent-engine.model-continuation-budget,embeddable-agent-engine.budget-exhaustion,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 1.7 Add failing `clankers-engine` reducer tests proving zero budget returns `EngineRejection::InvalidBudget` with unchanged state, no effects, and `terminal_failure: None`. [covers=embeddable-agent-engine.zero-budget,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 1.8 Add failing `clankers-engine` reducer tests proving `StopReason::MaxTokens` appends assistant content before `BusyChanged(false)` / `TurnFinished(MaxTokens)` with no retry, tool, follow-up model, or terminal-failure output. [covers=embeddable-agent-engine.max-tokens,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 1.9 Add failing `clankers-engine` reducer tests for invalid retry feedback: mismatched request IDs return `CorrelationMismatch`; wrong-phase retry failure, retry-ready, model-success feedback, duplicate scheduled failure, model-success/model-failure feedback during retry-wait before retry-ready, and post-terminal retry-ready/model-success/model-failure feedback return `InvalidPhase`; all preserve unchanged state and emit no effects. [covers=embeddable-agent-engine.invalid-retry-feedback,embeddable-agent-engine.reducer-retry-tests]
- [ ] 1.10 Before starting engine implementation, run `RUSTC_WRAPPER= cargo test -p clankers-engine` and capture the expected failing reducer-test output in `openspec/changes/engine-retry-stop-policy/evidence/reducer-tests-red.md` with evidence fields: `Artifact-Type`, `Evidence-ID`, `Task-ID`, `Covers`, `Creator`, `Created`, `Status`, `Command`, and `Output`. [covers=embeddable-agent-engine.reducer-retry-tests,embeddable-agent-engine.reducer-budget-token-tests]

## 2. Engine retry and stop-policy state

- [ ] 2.1 Extend engine-native state/input/template/outcome types with retry policy, retry attempts, retry-waiting phase/input, host-classified `EngineTerminalFailure { message, status, retryable }`, model-continuation budget, `EngineRejection::InvalidBudget`, and `EngineEffect::ScheduleRetry { request_id, delay }`. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.engine-native-payloads]
- [ ] 2.2 Implement retryable model failure authorization, `ScheduleRetry`, retry-ready request re-emission, retry attempt accounting, retry-counter reset on successful model feedback, fresh retry budget on follow-up model requests, and no canonical or shell-visible message mutation when scheduling retry work. [covers=embeddable-agent-engine.retry-scheduling,embeddable-agent-engine.retry-budget-reset,embeddable-agent-engine.retry-defaults,embeddable-agent-engine.reducer-retry-tests,turn-level-retry.no-duplicate-messages,turn-level-retry.failed-attempt-no-message-mutation]
- [ ] 2.3 Implement non-retryable model failure and retry exhaustion terminalization with `EngineTerminalFailure { message, status, retryable }` populated from the latest host-supplied failure details, `BusyChanged(false)`, failure notice, and `TurnFinished(Stop)` ordering, while emitting no `ScheduleRetry`, no `RequestModel`, and leaving canonical messages unchanged on those failed model attempts. [covers=embeddable-agent-engine.non-retryable-terminalization,embeddable-agent-engine.retry-exhaustion-terminalization]
- [ ] 2.4 Implement invalid retry-feedback reducer paths so mismatched request IDs return `CorrelationMismatch`, wrong-phase/duplicate/post-terminal feedback returns `InvalidPhase`, and all such rejections preserve state unchanged with no effects. [covers=embeddable-agent-engine.invalid-retry-feedback,embeddable-agent-engine.reducer-retry-tests]
- [ ] 2.5 Implement `CancelTurn { reason: "turn cancelled" }` handling in the retry-waiting phase so scheduled retry work is cleared, `BusyChanged(false)`, `Notice { message: "turn cancelled" }`, then `TurnFinished(Stop)` are emitted, no model request is executed, and later feedback for the cancelled request is rejected without mutation or effects. [covers=embeddable-agent-engine.retry-wait-cancellation,turn-level-retry.cancellation-during-backoff,turn-level-retry.cancelled-retry-no-attempt]
- [ ] 2.6 Implement continuation budget checks, within-budget follow-up `RequestModel` emission, accepted-tool-feedback retention, pending-tool cleanup, exact budget-exhaustion notice `"engine model request slot budget exhausted"`, `terminal_failure: None`, and no `RequestModel` on budget exhaustion while preserving existing normal stop and tool-use behavior. [covers=embeddable-agent-engine.model-continuation-budget,embeddable-agent-engine.budget-exhaustion]
- [ ] 2.7 Implement exact zero-budget rejection (`InvalidBudget`, unchanged state, no effects, `terminal_failure: None`). [covers=embeddable-agent-engine.zero-budget,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 2.8 Implement explicit `StopReason::MaxTokens` terminalization with accepted assistant content and no retry/tool/follow-up model effects or terminal failure. [covers=embeddable-agent-engine.max-tokens,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 2.9 Keep all delay/backoff and budget values named and deterministic; do not introduce async runtime, Tokio, provider-shaped `CompletionRequest`, daemon/TUI/protocol types, timestamps, shell-generated message IDs, or prompt-assembly dependencies into `clankers-engine`. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.engine-native-payloads]
- [ ] 2.10 Route normal stop, max-token stop, non-retryable model failure, retry exhaustion, budget exhaustion, and cancellation through shared engine-owned terminalization helpers so terminal ordering and `terminal_failure` semantics stay centralized. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.non-retryable-terminalization,embeddable-agent-engine.retry-exhaustion-terminalization,embeddable-agent-engine.budget-exhaustion,embeddable-agent-engine.retry-wait-cancellation,embeddable-agent-engine.max-tokens]

## 3. Runtime adapter migration

- [ ] 3.1 Update `clankers-agent::turn` to classify provider/model failures before engine submission, populate engine failure input, retain the original structured `AgentError` sidecar for final return, and stop owning retry authorization after submission. [covers=embeddable-agent-engine.host-classification,turn-level-retry.engine-authoritative,turn-level-retry.non-retryable,turn-level-retry.structured-agent-error]
- [ ] 3.2 Update `clankers-agent::turn` to execute `EngineEffect::ScheduleRetry`, sleep for the engine-provided delay, report retry-ready only after the delay completes, and execute the re-emitted `RequestModel`. [covers=embeddable-agent-engine.retry-scheduling,embeddable-agent-engine.retry-defaults,embeddable-agent-engine.reducer-retry-tests,turn-level-retry.engine-authoritative,turn-level-retry.transient-retry]
- [ ] 3.3 Propagate zero-budget `EngineRejection::InvalidBudget` through the existing engine-rejection error path without starting provider I/O. [covers=embeddable-agent-engine.zero-budget,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 3.4 Update `crates/clankers-agent/src/lib.rs` normal and orchestrated turn-config paths to pass named normal-turn total slot budget `25` and orchestration follow-up phase total slot budget `10` into engine configuration; both totals include the initial model request for that turn/phase. [covers=embeddable-agent-engine.model-continuation-budget,embeddable-agent-engine.budget-exhaustion]
- [ ] 3.5 Preserve cancellation during retry backoff by reporting `CancelTurn { reason: "turn cancelled" }` through the engine cancellation path, not reporting retry-ready after cancellation, not executing another model/provider attempt, and surfacing `AgentError::Cancelled`. [covers=embeddable-agent-engine.retry-wait-cancellation,turn-level-retry.cancellation-during-backoff,turn-level-retry.cancelled-retry-no-attempt]
- [ ] 3.6 Preserve shell-visible messages, usage updates, hooks, model switching, provider streaming, tool execution, and event emission as runtime-shell concerns around engine-owned decisions. [covers=embeddable-agent-engine.adapter-parity-rails]

## 4. Boundary rails and verification

Runtime adapter tests added by tasks 4.3–4.8 must live under a `clankers-agent` test module or test-name prefix matched by the `engine_retry_stop_policy` cargo filter used in task 4.10.

- [ ] 4.1 Extend parity or FCIS boundary rails with a deterministic non-test source inventory over `crates/clankers-agent/src/{lib.rs,turn/mod.rs,turn/execution.rs}` so validation fails if those files reintroduce retry-budget/backoff constants, retry-delay arithmetic, local retry authorization branches after host classification, direct `config.max_turns` continuation loops/branches, `StopReason::MaxTokens` terminal-policy branches, or provider/request-shaped retry payload construction outside the existing `turn/execution.rs` conversion seam; allow only shell execution of engine effects, provider stop-string parsing, provider request conversion in `turn/execution.rs`, host-side `AgentError::is_retryable()` classification used only to populate engine failure input, and named budget constants in `lib.rs` that feed engine configuration. [covers=embeddable-agent-engine.adapter-rail]
- [ ] 4.2 Add a deterministic `clankers-engine` surface rail that inventories public/non-test engine inputs, effects, outcomes, dependencies, and retry/budget/stop payloads, failing if they introduce provider-shaped `CompletionRequest`, daemon/TUI/protocol types, Tokio handles, timestamps, shell-generated message IDs, shell request construction, or prompt-assembly dependencies. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.engine-native-payloads]
- [ ] 4.3 Add runtime adapter tests proving retryable recovery, retry exhaustion terminal ordering, non-retryable terminal ordering, original structured `AgentError` propagation, no extra provider/model attempt, and no shell-visible message mutation for non-retryable and retry-exhausted failures while using engine-owned decisions. [covers=embeddable-agent-engine.adapter-rail,turn-level-retry.transient-retry,turn-level-retry.retry-exhaustion,turn-level-retry.non-retryable,turn-level-retry.structured-agent-error,embeddable-agent-engine.non-retryable-terminalization,embeddable-agent-engine.retry-exhaustion-terminalization]
- [ ] 4.4 Add runtime adapter tests proving cancellation during retry backoff reports `CancelTurn { reason: "turn cancelled" }`, does not report retry-ready, does not execute another provider/model attempt, and returns `AgentError::Cancelled`. [covers=embeddable-agent-engine.adapter-rail,embeddable-agent-engine.retry-wait-cancellation,turn-level-retry.cancellation-during-backoff,turn-level-retry.cancelled-retry-no-attempt]
- [ ] 4.5 Add runtime adapter tests proving failed retry attempts, non-retryable model failures, and retry-exhausted model failures do not duplicate or mutate shell-visible messages. [covers=embeddable-agent-engine.adapter-rail,turn-level-retry.no-duplicate-messages,turn-level-retry.failed-attempt-no-message-mutation]
- [ ] 4.6 Add runtime adapter tests proving exact zero-budget rejection avoids provider I/O and surfaces through the engine-rejection path. [covers=embeddable-agent-engine.adapter-rail,embeddable-agent-engine.zero-budget,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 4.7 Add runtime adapter tests proving within-budget tool feedback emits a follow-up model request, budget exhaustion terminalizes through engine policy with exact budget notice, `terminal_failure: None`, clears pending tool work, emits no follow-up model request, and named total slot-budget constants `25` / `10` feed engine configuration including the initial model request for the turn/phase. [covers=embeddable-agent-engine.adapter-rail,embeddable-agent-engine.model-continuation-budget,embeddable-agent-engine.budget-exhaustion]
- [ ] 4.8 Add runtime adapter tests proving `StopReason::MaxTokens` preserves assistant content while terminalizing with no retry/tool/follow-up model effects. [covers=embeddable-agent-engine.adapter-rail,embeddable-agent-engine.max-tokens,embeddable-agent-engine.reducer-budget-token-tests]
- [ ] 4.9 Add focused regression coverage or cite existing focused tests proving usage updates, hooks, model switching, provider streaming, tool execution, and event emission remain runtime-shell concerns and are not moved into `clankers-engine`; list the exact cargo test filters or static rail names that prove each cited behavior. [covers=embeddable-agent-engine.adapter-parity-rails]
- [ ] 4.10 Run `RUSTC_WRAPPER= cargo test -p clankers-engine` so all reducer tests execute, `RUSTC_WRAPPER= cargo test -p clankers-agent --lib engine_retry_stop_policy` for the focused agent retry/budget adapter tests, all exact test filters or rails listed in 4.9, and `RUSTC_WRAPPER= cargo test -p clankers-controller --test fcis_shell_boundaries`; capture machine-produced output in `openspec/changes/engine-retry-stop-policy/evidence/validation-suite.md` with evidence fields: `Artifact-Type`, `Evidence-ID`, `Task-ID`, `Covers`, `Creator`, `Created`, `Status`, `Command`, and `Output`. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.adapter-parity-rails,turn-level-retry.engine-authoritative,turn-level-retry.no-duplicate-messages,turn-level-retry.cancellation-during-backoff]
- [ ] 4.11 Rerun `openspec validate engine-retry-stop-policy --strict` plus the OpenSpec proposal, design, and tasks gates after implementation evidence exists; capture all four outputs in `openspec/changes/engine-retry-stop-policy/evidence/final-openspec-gates.md` with evidence fields: `Artifact-Type`, `Evidence-ID`, `Task-ID`, `Covers`, `Creator`, `Created`, `Status`, `Command`, and `Output`; then update tasks only when evidence paths are real. [covers=embeddable-agent-engine.retry-stop-policy-owned,embeddable-agent-engine.adapter-parity-rails,turn-level-retry.engine-authoritative,turn-level-retry.no-duplicate-messages,turn-level-retry.cancellation-during-backoff]
