[package]
name = "clankers-tui-types"
version = "0.1.0"
edition = "2024"

[dependencies]
clanker-tui-types = { workspace = true }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(dylint_lib, values("tigerstyle"))'] }
