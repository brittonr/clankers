[package]
name = "clankers-merge"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Re-exports graggle (extracted crate)"

[dependencies]
graggle = { git = "https://github.com/brittonr/graggle" }

[lints]
workspace = true
