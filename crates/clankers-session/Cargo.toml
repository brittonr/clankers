[package]
name = "clankers-session"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Session persistence and tree management for agent conversations"

[dependencies]
clanker-message = { workspace = true, features = ["transcript-compat"] }
clankers-artifacts = { version = "0.1.0", path = "../clankers-artifacts" }
automerge = "0.7"
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
pretty_assertions = "1.0"
tempfile = "3"

[lints]
workspace = true
