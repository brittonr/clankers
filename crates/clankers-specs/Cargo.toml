[package]
name = "clankers-specs"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Thin re-export wrapper for openspec (extracted crate)"

[dependencies]
openspec = { path = "../../../openspec" }
