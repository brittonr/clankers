## Phase 1: Baseline and Extraction

- [x] [serial] Inventory current responsibilities in turn/mod.rs and record baseline behavior fixtures. Evidence: inspected `turn/mod.rs` seams and kept existing turn/agent regression fixtures as the baseline rail.
- [x] [depends:baseline] Extract pure turn-state/policy helpers for phase transitions, retry/cancellation decisions, and tool-feedback routing. Evidence: extracted `turn/policy.rs`; `nix develop -c env CARGO_TARGET_DIR=target/hermes-agent-loop cargo nextest run -p clankers-agent turn --no-fail-fast` passed 59/59.
- [x] [depends:baseline] Extract provider streaming and completion request adaptation behind focused modules with parity tests. Evidence: preserved existing `turn/execution.rs` adaptation boundary and extracted message/stream collection helpers into `turn/message.rs`; turn tests passed 59/59.
- [x] [depends:baseline] Extract transcript/session/usage side-effect shells and run targeted turn-loop plus agent tests. Evidence: kept `turn/transcript.rs` and `turn/usage.rs` as side-effect shells and extracted host adapter wiring into `turn/adapters.rs`; `nix develop -c env CARGO_TARGET_DIR=target/hermes-agent-loop cargo nextest run -p clankers-agent agent --no-fail-fast` passed 17/17.
- [x] [serial] Run cargo fmt, targeted nextest filters for turn/agent, cargo check --tests for touched crates, openspec validate, and git diff --check. Evidence: `cargo fmt -p clankers-agent`; `nix develop -c env CARGO_TARGET_DIR=target/hermes-agent-loop cargo check --tests -p clankers-agent`; targeted nextest `turn` and `agent`; `cargo fmt --check -p clankers-agent`; `openspec validate decompose-agent-turn-loop --strict --json`; `git diff --check` all passed.
